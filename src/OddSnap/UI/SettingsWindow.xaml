<Window x:Class="OddSnap.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:OddSnap.UI.Controls"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="OddSnap"
        Height="720" Width="1080"
        MinHeight="560" MinWidth="900"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        Icon="pack://application:,,,/Assets/oddsnap.ico">

    <Window.Resources>
        <!-- Theme brushes (ThemeTextPrimaryBrush, ThemeCardBrush, …) are published
             into this window's resources by ApplyThemeColors() during construction,
             sourced from the UI.Theme token set. App-level copies exist as parse-time
             fallback via Theme.ApplyTo(Application.Current.Resources). Do not add
             hardcoded brush values here — they would be dark-only and drift. -->
        <sys:Double x:Key="SettingsControlHeight">30</sys:Double>
        <Thickness x:Key="SettingsControlPadding">10,5</Thickness>
        <sys:Double x:Key="SettingsControlFontSize">12</sys:Double>
        <sys:Double x:Key="SettingsControlMinWidth">88</sys:Double>

        <Style x:Key="TabPill" TargetType="RadioButton">
            <Setter Property="FocusVisualStyle" Value="{StaticResource OddSnapFocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Padding="12,5" CornerRadius="6" Cursor="Hand" Background="Transparent">
                            <TextBlock x:Name="Tb" Text="{TemplateBinding Content}" FontSize="11.5"
                                       FontFamily="Segoe UI Variable Text"
                                       Foreground="{DynamicResource ThemeTextPrimaryBrush}" Opacity="0.55"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabActiveBrush}"/>
                                <Setter TargetName="Tb" Property="Opacity" Value="1"/>
                                <Setter TargetName="Tb" Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                                <Setter TargetName="Tb" Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SubTab" TargetType="RadioButton">
            <Setter Property="FocusVisualStyle" Value="{StaticResource OddSnapFocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Padding="10,4" CornerRadius="5" Cursor="Hand" Background="Transparent">
                            <TextBlock x:Name="Tb" Text="{TemplateBinding Content}" FontSize="11"
                                       FontFamily="Segoe UI Variable Text"
                                       Foreground="{DynamicResource ThemeTextPrimaryBrush}" Opacity="0.5"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                                <Setter TargetName="Tb" Property="Opacity" Value="0.92"/>
                                <Setter TargetName="Tb" Property="FontWeight" Value="Medium"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Tb" Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Text"/>
            <Setter Property="Opacity" Value="0.96"/>
            <Setter Property="Margin" Value="0,4,0,6"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>

        <Style x:Key="NavItem" TargetType="RadioButton">
            <Setter Property="FocusVisualStyle" Value="{StaticResource OddSnapFocusVisual}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Border x:Name="Bd" Padding="12,0" Height="37" CornerRadius="6" Cursor="Hand" Background="Transparent"
                                    Margin="0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="28"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="Icon" Grid.Column="0" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                               FontSize="16" Text="&#xE713;" Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                               Opacity="0.74" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="Tb" Grid.Column="1" Text="{TemplateBinding Content}" FontSize="13"
                                               FontFamily="Segoe UI Variable Text"
                                               Foreground="{DynamicResource ThemeTextPrimaryBrush}" Opacity="0.82"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <!-- Win11-style selection indicator -->
                            <Rectangle x:Name="Ind" Width="3" Height="16" RadiusX="1.5" RadiusY="1.5"
                                       HorizontalAlignment="Left" VerticalAlignment="Center"
                                       Fill="{DynamicResource ThemeAccentBrush}" Opacity="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Content" Value="General">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE9E9;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="Toast">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE7F4;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="Tools">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE90F;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="Toolbar">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE7C9;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="Capture">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE722;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="Recording">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE714;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="OCR">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE8B7;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="History">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE81C;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="Uploads">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE898;"/>
                            </Trigger>
                            <Trigger Property="Content" Value="About">
                                <Setter TargetName="Icon" Property="Text" Value="&#xE946;"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
                                <Setter TargetName="Tb" Property="Opacity" Value="1"/>
                                <Setter TargetName="Tb" Property="FontWeight" Value="SemiBold"/>
                                <Setter TargetName="Icon" Property="Opacity" Value="0.92"/>
                                <Setter TargetName="Ind" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                                <Setter TargetName="Tb" Property="Opacity" Value="0.9"/>
                                <Setter TargetName="Icon" Property="Opacity" Value="0.84"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InfoText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Text"/>
            <Setter Property="Opacity" Value="0.68"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource ChromeButtonBase}">
            <Setter Property="Height" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="MinHeight" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="MinWidth" Value="{StaticResource SettingsControlMinWidth}"/>
            <Setter Property="Padding" Value="{StaticResource SettingsControlPadding}"/>
            <Setter Property="FontSize" Value="{StaticResource SettingsControlFontSize}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="Padding" Value="{StaticResource SettingsControlPadding}"/>
            <Setter Property="FontSize" Value="{StaticResource SettingsControlFontSize}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="MaxDropDownHeight" Value="280"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid MinWidth="{TemplateBinding MinWidth}"
                              SnapsToDevicePixels="True">
                            <ToggleButton x:Name="ToggleSite"
                                          MinWidth="{TemplateBinding MinWidth}"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Border x:Name="Bd"
                                        MinWidth="{TemplateBinding MinWidth}"
                                        MinHeight="{TemplateBinding MinHeight}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="3"
                                        Padding="{TemplateBinding Padding}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="18"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter x:Name="ContentSite"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                          IsHitTestVisible="False"
                                                          Margin="0,0,6,0"
                                                          RecognizesAccessKey="True"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                        <Path Grid.Column="1"
                                              Width="8"
                                              Height="4"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Data="M 0 0 L 4 4 L 8 0 Z"
                                              Fill="{DynamicResource ThemeTextSecondaryBrush}"/>
                                    </Grid>
                                </Border>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Margin="0,4,0,0"
                                        Background="{DynamicResource ThemeCardBrush}"
                                        BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        SnapsToDevicePixels="True">
                                    <ScrollViewer Padding="4"
                                                  CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#35FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Padding" Value="9,5"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabActiveBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="Padding" Value="{StaticResource SettingsControlPadding}"/>
            <Setter Property="FontSize" Value="{StaticResource SettingsControlFontSize}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="MinHeight" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="Padding" Value="{StaticResource SettingsControlPadding}"/>
            <Setter Property="FontSize" Value="{StaticResource SettingsControlFontSize}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="ToolTip" Value="Hidden for safety. Paste a new value to replace it."/>
            <Setter Property="AutomationProperties.HelpText" Value="Hidden for safety. Paste a new value to replace it."/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Width" Value="42"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Track"
                                Width="40"
                                Height="20"
                                CornerRadius="10"
                                Background="#00000000"
                                BorderBrush="{DynamicResource ThemeTextSecondaryBrush}"
                                BorderThickness="1"
                                SnapsToDevicePixels="True">
                            <Ellipse x:Name="Thumb"
                                     Width="12"
                                     Height="12"
                                     Margin="4,0"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Center"
                                     Fill="{DynamicResource ThemeTextSecondaryBrush}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{DynamicResource ThemeAccentBrush}"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="{DynamicResource ThemeAccentBrush}"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Track" Property="BorderBrush" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Track" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="14,9"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Background" Value="{DynamicResource ThemeCardBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="SettingRow" TargetType="Grid">
            <Setter Property="MinHeight" Value="52"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <Style x:Key="SettingTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Text"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SettingDescription" TargetType="TextBlock" BasedOn="{StaticResource InfoText}">
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="Opacity" Value="0.72"/>
            <Setter Property="Margin" Value="0,3,0,0"/>
        </Style>

        <Style x:Key="SettingsHelperText" TargetType="TextBlock" BasedOn="{StaticResource InfoText}">
            <Setter Property="FontSize" Value="10.5"/>
            <Setter Property="Opacity" Value="0.56"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="SettingsStatusText" TargetType="TextBlock" BasedOn="{StaticResource InfoText}">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Opacity" Value="0.58"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <Style x:Key="SettingsProgressText" TargetType="TextBlock" BasedOn="{StaticResource InfoText}">
            <Setter Property="FontSize" Value="10.5"/>
            <Setter Property="Opacity" Value="0.5"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>

        <Style x:Key="SettingIconFrame" TargetType="Border">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="CornerRadius" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SettingIconGlyph" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Opacity" Value="0.92"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="AboutActionLink" TargetType="Hyperlink">
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="TextDecorations" Value="{x:Null}"/>
        </Style>

        <Style x:Key="AboutActionRow" TargetType="Border">
            <Setter Property="CornerRadius" Value="7"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Background" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="AboutIconFrame" TargetType="Border">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="CornerRadius" Value="7"/>
            <Setter Property="Background" Value="{DynamicResource ThemeTabActiveBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InlineTagChip" TargetType="Border">
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Background" Value="{DynamicResource ThemeTabActiveBrush}"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
        </Style>

        <Style x:Key="InlineTagChipDanger" TargetType="Border" BasedOn="{StaticResource InlineTagChip}">
            <Setter Property="Background" Value="#30EF4444"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource OddSnapDangerButton}">
            <Setter Property="Height" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="MinHeight" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="MinWidth" Value="108"/>
            <Setter Property="Padding" Value="{StaticResource SettingsControlPadding}"/>
            <Setter Property="FontSize" Value="{StaticResource SettingsControlFontSize}"/>
        </Style>

        <Style x:Key="HistoryActionButton" TargetType="Button" BasedOn="{StaticResource ChromeButtonBase}">
            <Setter Property="Height" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="MinHeight" Value="{StaticResource SettingsControlHeight}"/>
            <Setter Property="MinWidth" Value="{StaticResource SettingsControlMinWidth}"/>
            <Setter Property="Padding" Value="{StaticResource SettingsControlPadding}"/>
            <Setter Property="FontSize" Value="{StaticResource SettingsControlFontSize}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="HistoryDangerButton" TargetType="Button" BasedOn="{StaticResource DangerButton}">
            <Setter Property="MinWidth" Value="82"/>
        </Style>

        <Style x:Key="HistoryActionsMenuStyle" TargetType="ContextMenu">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="MinWidth" Value="156"/>
            <Setter Property="Background" Value="{DynamicResource ThemeCardBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HistoryActionsMenuItem" TargetType="MenuItem">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Text"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="MinWidth" Value="148"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Bd"
                                MinHeight="{TemplateBinding MinHeight}"
                                CornerRadius="4"
                                Background="Transparent"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ProviderIconFrame" TargetType="Border">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="CornerRadius" Value="7"/>
            <Setter Property="Background" Value="{DynamicResource ThemeTabActiveBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ProviderIconImage" TargetType="Image">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Stretch" Value="Uniform"/>
        </Style>

        <Style TargetType="{x:Type controls:ProviderSection}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type controls:ProviderSection}">
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <DockPanel x:Name="ProviderHeaderRow" Margin="0,0,0,12" LastChildFill="True">
                                    <ContentControl x:Name="HeaderExtrasHost"
                                                    DockPanel.Dock="Right"
                                                    Content="{TemplateBinding HeaderExtras}"
                                                    VerticalAlignment="Center"/>
                                    <Border x:Name="ProviderHeaderIconFrame" Style="{StaticResource SettingIconFrame}">
                                        <Image x:Name="ProviderHeaderIcon"
                                               Source="{TemplateBinding IconSource}"
                                               Width="16"
                                               Height="16"
                                               Stretch="Uniform"
                                               RenderOptions.BitmapScalingMode="HighQuality"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="{TemplateBinding Header}"
                                               FontSize="12.5"
                                               FontWeight="SemiBold"
                                               FontFamily="Segoe UI Variable Text"
                                               Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                               Opacity="0.82"
                                               VerticalAlignment="Center"/>
                                </DockPanel>
                                <ContentPresenter Content="{TemplateBinding Content}"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Header" Value="{x:Null}">
                                <Setter TargetName="ProviderHeaderRow" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="IconSource" Value="{x:Null}">
                                <Setter TargetName="ProviderHeaderIconFrame" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="HeaderExtras" Value="{x:Null}">
                                <Setter TargetName="HeaderExtrasHost" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

        <Border Margin="1" CornerRadius="12">
    <Border x:Name="OuterBorder" CornerRadius="12" ClipToBounds="True"
            BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:OddSnapTitleBar Grid.Row="0"
                                  Title="Settings"
                                  CloseRequested="TitleBar_CloseRequested"/>

        <!-- Sidebar + Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="232"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar navigation -->
            <Border Grid.Column="0" BorderBrush="{DynamicResource ThemeSeparatorBrush}" BorderThickness="0,0,1,0"
                    Padding="12,10,10,14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- App identity -->
                    <Grid Grid.Row="0" Margin="6,4,4,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image x:Name="SidebarLogo" Width="30" Height="30" VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                        <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="OddSnap" FontSize="13.5" FontWeight="SemiBold"
                                       FontFamily="Segoe UI Variable Text"
                                       Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                            <TextBlock x:Name="SidebarVersionText" Text="" FontSize="10.5"
                                       FontFamily="Segoe UI Variable Text" Margin="0,1,0,0"
                                       Foreground="{DynamicResource ThemeTextSecondaryBrush}"/>
                        </StackPanel>
                    </Grid>

                    <!-- Settings search -->
                    <Grid Grid.Row="1" Margin="2,0,2,10">
                        <Border Background="{DynamicResource ThemeInputBackgroundBrush}"
                                BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                                BorderThickness="1" CornerRadius="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE721;" FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                                           FontSize="12" Margin="9,0,0,0" VerticalAlignment="Center"
                                           Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                                           IsHitTestVisible="False"/>
                                <TextBox x:Name="SettingsSearchBox" Grid.Column="1"
                                         Background="Transparent" BorderThickness="0"
                                         Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                         CaretBrush="{DynamicResource ThemeTextPrimaryBrush}"
                                         FontSize="12" FontFamily="Segoe UI Variable Text"
                                         Padding="6,7" VerticalContentAlignment="Center"
                                         AutomationProperties.Name="Search settings"
                                         TextChanged="SettingsSearchBox_TextChanged"
                                         PreviewKeyDown="SettingsSearchBox_PreviewKeyDown"/>
                                <TextBlock x:Name="SettingsSearchHint" Grid.Column="1" Text="Find a setting"
                                           FontSize="12" FontFamily="Segoe UI Variable Text"
                                           Margin="10,0,0,0" VerticalAlignment="Center"
                                           Foreground="{DynamicResource ThemeMutedBrush}"
                                           IsHitTestVisible="False"/>
                            </Grid>
                        </Border>
                        <Popup x:Name="SettingsSearchPopup" PlacementTarget="{Binding ElementName=SettingsSearchBox}"
                               Placement="Bottom" VerticalOffset="6" HorizontalOffset="-24"
                               StaysOpen="True" AllowsTransparency="True">
                            <Border Background="{DynamicResource ThemeCardBrush}"
                                    BorderBrush="{DynamicResource ThemeWindowBorderBrush}"
                                    BorderThickness="1" CornerRadius="8" Padding="4"
                                    MinWidth="260" MaxWidth="320" MaxHeight="340">
                                <ListBox x:Name="SettingsSearchResults" Background="Transparent" BorderThickness="0"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         PreviewMouseLeftButtonUp="SettingsSearchResults_MouseUp"
                                         PreviewKeyDown="SettingsSearchResults_PreviewKeyDown">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border x:Name="ItemBd" CornerRadius="5" Padding="10,7" Margin="0,1"
                                                                Background="Transparent" Cursor="Hand">
                                                            <ContentPresenter/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="ItemBd" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="ItemBd" Property="Background" Value="{DynamicResource ThemeTabActiveBrush}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                </ListBox>
                            </Border>
                        </Popup>
                    </Grid>

                    <!-- Nav items -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel>
                            <RadioButton x:Name="SettingsTab" Content="General" GroupName="Main"
                                         IsChecked="True" Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <RadioButton x:Name="ToastTab" Content="Toast" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <RadioButton x:Name="HotkeysTab" Content="Tools" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <RadioButton x:Name="ToolbarTab" Content="Toolbar" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <RadioButton x:Name="CaptureTab" Content="Capture" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <RadioButton x:Name="RecordingTab" Content="Recording" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <RadioButton x:Name="OcrTab" Content="OCR" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="8,10,8,10"/>
                            <RadioButton x:Name="HistoryTab" Content="History" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <RadioButton x:Name="UploadsTab" Content="Uploads" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="8,10,8,10"/>
                            <RadioButton x:Name="AboutTab" Content="About" GroupName="Main"
                                         Click="TabChanged" Style="{StaticResource NavItem}"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Content area -->
            <Grid Grid.Column="1">
                <TextBlock x:Name="PageTitleText"
                           Text="General"
                           Margin="26,14,26,0"
                           FontSize="26"
                           FontWeight="SemiBold"
                           FontFamily="Segoe UI Variable Display"
                           Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                           Panel.ZIndex="10"
                           IsHitTestVisible="False"/>


            <!-- Tools (unified hotkeys + toolbar) -->
            <ScrollViewer x:Name="HotkeysPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="26,58,26,22">
                <StackPanel>
                    <CheckBox x:Name="ShowToolNumberBadgesCheck" Visibility="Collapsed"
                              Checked="ShowToolNumberBadgesCheck_Changed" Unchecked="ShowToolNumberBadgesCheck_Changed"/>
                    <StackPanel x:Name="ToolTogglePanel"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Toolbar layout -->
            <ScrollViewer x:Name="ToolbarPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="26,58,26,22">
                <StackPanel>
                    <TextBlock Text="Pinned" Style="{StaticResource SectionLabel}"/>
                    <Border Style="{StaticResource Card}" AllowDrop="True" Drop="ToolbarPinnedList_Drop" DragOver="ToolbarList_DragOver">
                        <StackPanel>
                            <StackPanel x:Name="ToolbarPinnedList"/>
                            <TextBlock x:Name="ToolbarPinnedEmptyText" Text="Drop toolbar items here to pin them." Style="{StaticResource InfoText}" Visibility="Collapsed"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="More" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}" AllowDrop="True" Drop="ToolbarMoreList_Drop" DragOver="ToolbarList_DragOver">
                        <StackPanel>
                            <StackPanel x:Name="ToolbarMoreList"/>
                            <TextBlock x:Name="ToolbarMoreEmptyText" Text="Unpinned items appear in the toolbar's More menu." Style="{StaticResource InfoText}" Visibility="Collapsed"/>
                        </StackPanel>
                    </Border>

                    <DockPanel Margin="0,12,0,0">
                        <TextBlock x:Name="ToolbarLayoutStatusText"
                                   DockPanel.Dock="Left"
                                   Style="{StaticResource SettingsStatusText}"
                                   Visibility="Collapsed"
                                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                   AutomationProperties.Name="Toolbar layout status"
                                   AutomationProperties.LiveSetting="Polite"
                                   VerticalAlignment="Center"/>
                        <Button x:Name="ResetToolbarLayoutBtn"
                                DockPanel.Dock="Right"
                                Content="Reset"
                                MinWidth="82"
                                HorizontalAlignment="Right"
                                AutomationProperties.Name="Reset toolbar layout"
                                ToolTip="Restore the default toolbar layout."
                                Click="ResetToolbarLayoutBtn_Click"/>
                    </DockPanel>
                </StackPanel>
            </ScrollViewer>

            <!-- Capture + Saving -->
            <ScrollViewer x:Name="CapturePanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="26,58,26,22">
                <StackPanel>
                    <TextBlock Text="Capture Tool" Style="{StaticResource SectionLabel}"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE722;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Default capture tool" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose which selection mode opens first." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="DefaultCaptureModeCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Default capture tool"
                                          ToolTip="Choose which selection mode opens first."
                                          SelectionChanged="DefaultCaptureModeCombo_SelectionChanged">
                                    <ComboBoxItem Content="Rectangle"
                                                  ToolTip="Drag from corner to corner."
                                                  AutomationProperties.Name="Rectangle selection"
                                                  AutomationProperties.HelpText="Start captures with the standard rectangular selection tool."/>
                                    <ComboBoxItem Content="Center"
                                                  ToolTip="Drag outward from a center point."
                                                  AutomationProperties.Name="Center selection"
                                                  AutomationProperties.HelpText="Start captures with a center-based selection tool."/>
                                    <ComboBoxItem Content="Freeform"
                                                  ToolTip="Draw an irregular capture outline."
                                                  AutomationProperties.Name="Freeform selection"
                                                  AutomationProperties.HelpText="Start captures with the freeform selection tool."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE1D9;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Center aspect ratio" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Optional ratio lock for center selection." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="CenterAspectRatioCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Center aspect ratio"
                                          ToolTip="Optional ratio lock for center selection."
                                          SelectionChanged="CenterAspectRatioCombo_SelectionChanged">
                                    <ComboBoxItem Content="Free"
                                                  ToolTip="Do not lock the center selection ratio."
                                                  AutomationProperties.Name="Free center ratio"
                                                  AutomationProperties.HelpText="Let center selection resize freely without an aspect-ratio lock."/>
                                    <ComboBoxItem Content="Square"
                                                  ToolTip="Lock center selection to a square."
                                                  AutomationProperties.Name="Square center ratio"
                                                  AutomationProperties.HelpText="Keep center selection width and height equal."/>
                                    <ComboBoxItem Content="16:9"
                                                  ToolTip="Lock center selection to widescreen landscape."
                                                  AutomationProperties.Name="16:9 center ratio"
                                                  AutomationProperties.HelpText="Keep center selection in a 16 to 9 landscape ratio."/>
                                    <ComboBoxItem Content="4:3"
                                                  ToolTip="Lock center selection to standard landscape."
                                                  AutomationProperties.Name="4:3 center ratio"
                                                  AutomationProperties.HelpText="Keep center selection in a 4 to 3 landscape ratio."/>
                                    <ComboBoxItem Content="3:2"
                                                  ToolTip="Lock center selection to photo landscape."
                                                  AutomationProperties.Name="3:2 center ratio"
                                                  AutomationProperties.HelpText="Keep center selection in a 3 to 2 landscape ratio."/>
                                    <ComboBoxItem Content="9:16"
                                                  ToolTip="Lock center selection to vertical portrait."
                                                  AutomationProperties.Name="9:16 center ratio"
                                                  AutomationProperties.HelpText="Keep center selection in a 9 to 16 portrait ratio."/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Selection overlay" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE7B3;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Show cursor in captures and recordings" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Include the pointer in saved media." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="ShowCursorCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Include the pointer in saved media"
                                          AutomationProperties.Name="Show cursor in captures and recordings"
                                          Checked="ShowCursorCheck_Changed" Unchecked="ShowCursorCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE890;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Show OddSnap UI in screenshots" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Include OddSnap windows, previews, and notifications in screenshots." Style="{StaticResource SettingDescription}" TextWrapping="Wrap"/>
                                </StackPanel>
                                <CheckBox x:Name="ShowOddSnapUiInScreenshotsCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Include OddSnap windows, previews, and notifications in screenshots"
                                          AutomationProperties.Name="Show OddSnap UI in screenshots"
                                          AutomationProperties.HelpText="Turn this off to keep OddSnap windows, previews, and notifications out of screenshots."
                                          Checked="ShowOddSnapUiInScreenshotsCheck_Changed" Unchecked="ShowOddSnapUiInScreenshotsCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE1D1;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Show crosshair guides" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Display alignment guides while selecting." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="CrosshairGuidesCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Show alignment guides while selecting"
                                          AutomationProperties.Name="Show crosshair guides"
                                          Checked="CrosshairGuidesCheck_Changed" Unchecked="CrosshairGuidesCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE721;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Show pixel magnifier while selecting" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Zoom the cursor area during selection." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="ShowCaptureMagnifierCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Zoom the cursor area during selection"
                                          AutomationProperties.Name="Show pixel magnifier while selecting"
                                          Checked="ShowCaptureMagnifierCheck_Changed" Unchecked="ShowCaptureMagnifierCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE7F4;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Span selection overlay across all monitors" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Use one overlay over the full virtual desktop." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="OverlayAllMonitorsCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Use one overlay across the full virtual desktop"
                                          AutomationProperties.Name="Span selection overlay across all monitors"
                                          Checked="OverlayAllMonitorsCheck_Changed" Unchecked="OverlayAllMonitorsCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE790;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="HDR capture compatibility" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Use the Windows color-managed capture path on HDR displays." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="HdrCaptureCompatibleModeCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Use the Windows color-managed capture path on HDR displays"
                                          AutomationProperties.Name="HDR capture compatibility"
                                          Checked="HdrCaptureCompatibleModeCheck_Changed" Unchecked="HdrCaptureCompatibleModeCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE790;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Annotation stroke and shadow" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Keep annotations readable on mixed backgrounds." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="AnnotationStrokeShadowCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Keep annotations readable on mixed backgrounds"
                                          AutomationProperties.Name="Annotation stroke and shadow"
                                          Checked="AnnotationStrokeShadowCheck_Changed" Unchecked="AnnotationStrokeShadowCheck_Changed"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Behavior" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE7C3;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Window detection" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Detect windows when hovering before selection." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="WindowDetectionCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Window detection"
                                          ToolTip="Detect windows when hovering before selection."
                                          SelectionChanged="WindowDetectionCombo_SelectionChanged">
                                    <ComboBoxItem Content="Off"
                                                  ToolTip="Ignore windows while selecting."
                                                  AutomationProperties.Name="Window detection off"
                                                  AutomationProperties.HelpText="Do not highlight or snap to windows during selection."/>
                                    <ComboBoxItem Content="Windows only"
                                                  ToolTip="Detect windows while hovering."
                                                  AutomationProperties.Name="Windows-only detection"
                                                  AutomationProperties.HelpText="Highlight detected windows during selection so captures can snap to them."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8A7;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Capture dock" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Position the capture toolbar around the screen." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="CaptureDockSideCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Capture dock"
                                          ToolTip="Position the capture toolbar around the screen."
                                          SelectionChanged="CaptureDockSideCombo_SelectionChanged">
                                    <ComboBoxItem Content="Top"
                                                  ToolTip="Show the capture toolbar along the top edge."
                                                  AutomationProperties.Name="Top capture dock"
                                                  AutomationProperties.HelpText="Place the capture toolbar at the top of the screen."/>
                                    <ComboBoxItem Content="Bottom"
                                                  ToolTip="Show the capture toolbar along the bottom edge."
                                                  AutomationProperties.Name="Bottom capture dock"
                                                  AutomationProperties.HelpText="Place the capture toolbar at the bottom of the screen."/>
                                    <ComboBoxItem Content="Left"
                                                  ToolTip="Show the capture toolbar along the left edge."
                                                  AutomationProperties.Name="Left capture dock"
                                                  AutomationProperties.HelpText="Place the capture toolbar on the left side of the screen."/>
                                    <ComboBoxItem Content="Right"
                                                  ToolTip="Show the capture toolbar along the right edge."
                                                  AutomationProperties.Name="Right capture dock"
                                                  AutomationProperties.HelpText="Place the capture toolbar on the right side of the screen."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE7F0;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Scrolling capture mode" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose whether scrolling capture collects frames itself or only when you click capture." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="ScrollingCaptureModeCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Scrolling capture mode"
                                          ToolTip="Choose whether scrolling capture collects frames itself or only when you click capture."
                                          SelectionChanged="ScrollingCaptureModeCombo_SelectionChanged">
                                    <ComboBoxItem Content="Automatic"
                                                  ToolTip="Let OddSnap collect scrolling frames automatically."
                                                  AutomationProperties.Name="Automatic scrolling capture"
                                                  AutomationProperties.HelpText="Automatically collect frames while scrolling capture is active."/>
                                    <ComboBoxItem Content="Manual"
                                                  ToolTip="Capture scrolling frames only when you click."
                                                  AutomationProperties.Name="Manual scrolling capture"
                                                  AutomationProperties.HelpText="Only collect a scrolling-capture frame when you press the capture button."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE930;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="After capture" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose what OddSnap does after saving the selection." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="AfterCaptureCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="After capture"
                                          ToolTip="Choose what OddSnap does after saving the selection."
                                          SelectionChanged="AfterCaptureCombo_SelectionChanged">
                                    <ComboBoxItem Content="Copy to clipboard"
                                                  ToolTip="Copy the capture without opening a preview."
                                                  AutomationProperties.Name="Copy after capture"
                                                  AutomationProperties.HelpText="Copy the saved capture to the clipboard and skip the preview window."/>
                                    <ComboBoxItem Content="Preview + Copy"
                                                  ToolTip="Open a preview and copy the capture."
                                                  AutomationProperties.Name="Preview and copy after capture"
                                                  AutomationProperties.HelpText="Open the preview window and also copy the saved capture to the clipboard."/>
                                    <ComboBoxItem Content="Preview only"
                                                  ToolTip="Open a preview without copying."
                                                  AutomationProperties.Name="Preview only after capture"
                                                  AutomationProperties.HelpText="Open the preview window without copying the saved capture to the clipboard."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE916;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Capture delay" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Wait before the capture overlay opens." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="CaptureDelayCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Capture delay"
                                          ToolTip="Wait before the capture overlay opens."
                                          SelectionChanged="CaptureDelayCombo_SelectionChanged">
                                    <ComboBoxItem Content="None"
                                                  ToolTip="Open capture immediately."
                                                  AutomationProperties.Name="No capture delay"
                                                  AutomationProperties.HelpText="Start the capture overlay immediately after choosing capture."/>
                                    <ComboBoxItem Content="3 seconds"
                                                  ToolTip="Wait 3 seconds before capture."
                                                  AutomationProperties.Name="3 second capture delay"
                                                  AutomationProperties.HelpText="Wait 3 seconds before opening the capture overlay."/>
                                    <ComboBoxItem Content="5 seconds"
                                                  ToolTip="Wait 5 seconds before capture."
                                                  AutomationProperties.Name="5 second capture delay"
                                                  AutomationProperties.HelpText="Wait 5 seconds before opening the capture overlay."/>
                                    <ComboBoxItem Content="10 seconds"
                                                  ToolTip="Wait 10 seconds before capture."
                                                  AutomationProperties.Name="10 second capture delay"
                                                  AutomationProperties.HelpText="Wait 10 seconds before opening the capture overlay."/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Saving" Style="{StaticResource SectionLabel}" Margin="4,18,0,9"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE74E;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Save screenshots to file" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Write captures to your configured folder." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="SaveToFileCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Write screenshots to the configured save folder"
                                          AutomationProperties.Name="Save screenshots to file"
                                          Checked="SaveToFileCheck_Changed" Unchecked="SaveToFileCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8AC;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Ask for file name every time" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Prompt before writing each saved capture." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="AskFileNameCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Prompt for a file name before each saved capture"
                                          AutomationProperties.Name="Ask for file name every time"
                                          Checked="AskFileNameCheck_Changed" Unchecked="AskFileNameCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8D2;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="File name pattern" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Use tokens to build capture file names." Style="{StaticResource SettingDescription}"/>
                                    <TextBox x:Name="FileNameTemplateBox" MinWidth="145" MaxWidth="300"
                                             HorizontalAlignment="Stretch" Margin="0,8,0,0"
                                             HorizontalScrollBarVisibility="Auto"
                                             ToolTip="Use tokens to build capture file names."
                                             AutomationProperties.Name="File name pattern"
                                             PreviewKeyDown="FileNameTemplateBox_PreviewKeyDown"
                                             TextChanged="FileNameTemplateBox_TextChanged"
                                             Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                             BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                    <WrapPanel x:Name="FileNameTokenPanel" Margin="0,8,0,-6"/>
                                    <TextBlock x:Name="FileNameTemplatePreviewText" FontSize="11" Margin="0,6,0,0"
                                               Foreground="{DynamicResource ThemeTextSecondaryBrush}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock x:Name="CaptureSavePreferenceStatusText"
                                               Style="{StaticResource SettingsStatusText}"
                                               Visibility="Collapsed"
                                               Margin="0,6,0,0"/>
                                </StackPanel>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8B7;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Create monthly subfolders" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Organize captures into year-month folders." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="MonthlyFoldersCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Organize captures into year-month folders"
                                          AutomationProperties.Name="Create monthly subfolders"
                                          Checked="MonthlyFoldersCheck_Changed" Unchecked="MonthlyFoldersCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8B7;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Save folder" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose where image captures are stored." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <Button x:Name="BrowseSaveDirBtn" Content="Browse" Grid.Column="2" MinWidth="90"
                                        VerticalAlignment="Center" ToolTip="Choose save folder"
                                        AutomationProperties.Name="Choose save folder"
                                        Cursor="Hand" Click="BrowseButton_Click"/>
                            </Grid>
                            <Grid x:Name="SaveDirPanel" Style="{StaticResource SettingRow}" Margin="34,6,0,0" Visibility="Collapsed">
                                <TextBox x:Name="SaveDirBox" IsReadOnly="True"
                                         MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch"
                                         HorizontalScrollBarVisibility="Auto"
                                         ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                         AutomationProperties.Name="Current save folder"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                         BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8A5;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Default format" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Set the file type for image captures." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="CaptureFormatCombo" Grid.Column="2" MinWidth="125"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Default format"
                                          ToolTip="Set the file type for image captures."
                                          SelectionChanged="CaptureFormatCombo_SelectionChanged">
                                    <ComboBoxItem Content="PNG"
                                                  ToolTip="Save lossless images with transparency."
                                                  AutomationProperties.Name="PNG image format"
                                                  AutomationProperties.HelpText="Save captures as lossless PNG files, including transparency when available."/>
                                    <ComboBoxItem Content="JPG"
                                                  ToolTip="Save smaller photos without transparency."
                                                  AutomationProperties.Name="JPG image format"
                                                  AutomationProperties.HelpText="Save captures as compressed JPG files for smaller image sizes."/>
                                    <ComboBoxItem Content="BMP"
                                                  ToolTip="Save uncompressed bitmap images."
                                                  AutomationProperties.Name="BMP image format"
                                                  AutomationProperties.HelpText="Save captures as uncompressed BMP files for maximum compatibility."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid x:Name="JpegQualityPanel" Style="{StaticResource SettingRow}" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE9D2;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="JPG quality" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Balance quality against file size." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="JpegQualityCombo" Grid.Column="2" MinWidth="125"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="JPG quality"
                                          ToolTip="Balance quality against file size."
                                          SelectionChanged="JpegQualityCombo_SelectionChanged">
                                    <ComboBoxItem Content="100 - Best" Tag="100"
                                                  ToolTip="Use maximum JPG quality."
                                                  AutomationProperties.Name="100 JPG quality"
                                                  AutomationProperties.HelpText="Save JPG captures at maximum quality with the largest file size."/>
                                    <ComboBoxItem Content="90 - High" Tag="90"
                                                  ToolTip="Use high JPG quality."
                                                  AutomationProperties.Name="90 JPG quality"
                                                  AutomationProperties.HelpText="Save JPG captures at high quality with moderate compression."/>
                                    <ComboBoxItem Content="85 - Balanced" Tag="85"
                                                  ToolTip="Balance JPG quality and file size."
                                                  AutomationProperties.Name="85 JPG quality"
                                                  AutomationProperties.HelpText="Save JPG captures with balanced quality and file size."/>
                                    <ComboBoxItem Content="75 - Smaller" Tag="75"
                                                  ToolTip="Use smaller JPG files."
                                                  AutomationProperties.Name="75 JPG quality"
                                                  AutomationProperties.HelpText="Save JPG captures with stronger compression for smaller files."/>
                                    <ComboBoxItem Content="60 - Tiny" Tag="60"
                                                  ToolTip="Use the smallest JPG files."
                                                  AutomationProperties.Name="60 JPG quality"
                                                  AutomationProperties.HelpText="Save JPG captures with heavy compression for tiny files."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE740;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Max image size" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Resize oversized captures after saving." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="CaptureSizeCombo" Grid.Column="2" MinWidth="125"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Max image size"
                                          ToolTip="Resize oversized captures after saving."
                                          SelectionChanged="CaptureSizeCombo_SelectionChanged">
                                    <ComboBoxItem Content="Original" Tag="0"
                                                  ToolTip="Keep the original capture size."
                                                  AutomationProperties.Name="Original capture size"
                                                  AutomationProperties.HelpText="Save captures without resizing the longest edge."/>
                                    <ComboBoxItem Content="2160p" Tag="2160"
                                                  ToolTip="Limit captures to 2160p."
                                                  AutomationProperties.Name="2160p max image size"
                                                  AutomationProperties.HelpText="Resize oversized captures so the longest edge is at most 2160 pixels."/>
                                    <ComboBoxItem Content="1440p" Tag="1440"
                                                  ToolTip="Limit captures to 1440p."
                                                  AutomationProperties.Name="1440p max image size"
                                                  AutomationProperties.HelpText="Resize oversized captures so the longest edge is at most 1440 pixels."/>
                                    <ComboBoxItem Content="1080p" Tag="1080"
                                                  ToolTip="Limit captures to 1080p."
                                                  AutomationProperties.Name="1080p max image size"
                                                  AutomationProperties.HelpText="Resize oversized captures so the longest edge is at most 1080 pixels."/>
                                    <ComboBoxItem Content="720p" Tag="720"
                                                  ToolTip="Limit captures to 720p."
                                                  AutomationProperties.Name="720p max image size"
                                                  AutomationProperties.HelpText="Resize oversized captures so the longest edge is at most 720 pixels."/>
                                    <ComboBoxItem Content="480p" Tag="480"
                                                  ToolTip="Limit captures to 480p."
                                                  AutomationProperties.Name="480p max image size"
                                                  AutomationProperties.HelpText="Resize oversized captures so the longest edge is at most 480 pixels."/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="History" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE81C;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Save capture history" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Keep captures available in the History page." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="SaveHistoryCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          ToolTip="Keep captures available in the History page"
                                          AutomationProperties.Name="Save capture history"
                                          Checked="SaveHistoryCheck_Changed" Unchecked="SaveHistoryCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE916;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Auto-clear history" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Automatically prune old capture entries." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="HistoryRetentionCombo" Grid.Column="2" MinWidth="125"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Auto-clear history"
                                          ToolTip="Automatically prune old capture entries."
                                          SelectionChanged="HistoryRetentionCombo_SelectionChanged">
                                    <ComboBoxItem Content="Never"
                                                  ToolTip="Keep history until manually cleared"
                                                  AutomationProperties.Name="Never auto-clear history"
                                                  AutomationProperties.HelpText="Keep saved capture history until you clear it manually."/>
                                    <ComboBoxItem Content="1 day"
                                                  ToolTip="Keep history for 1 day"
                                                  AutomationProperties.Name="Keep history for 1 day"
                                                  AutomationProperties.HelpText="Automatically remove capture history older than 1 day."/>
                                    <ComboBoxItem Content="7 days"
                                                  ToolTip="Keep history for 7 days"
                                                  AutomationProperties.Name="Keep history for 7 days"
                                                  AutomationProperties.HelpText="Automatically remove capture history older than 7 days."/>
                                    <ComboBoxItem Content="30 days"
                                                  ToolTip="Keep history for 30 days"
                                                  AutomationProperties.Name="Keep history for 30 days"
                                                  AutomationProperties.HelpText="Automatically remove capture history older than 30 days."/>
                                    <ComboBoxItem Content="3 months"
                                                  ToolTip="Keep history for 3 months"
                                                  AutomationProperties.Name="Keep history for 3 months"
                                                  AutomationProperties.HelpText="Automatically remove capture history older than 3 months."/>
                                </ComboBox>
                            </Grid>
                            <TextBlock x:Name="HistoryPreferenceStatusText"
                                       Style="{StaticResource SettingsStatusText}"
                                       Visibility="Collapsed"
                                       Margin="34,5,0,0"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- Recording -->
            <ScrollViewer x:Name="RecordingPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="26,58,26,22">
                <StackPanel>
                    <TextBlock Text="Recording" Style="{StaticResource SectionLabel}"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE714;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Format" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose the video container for recordings." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="RecordingFormatCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          ToolTip="Choose the video container for recordings"
                                          AutomationProperties.Name="Recording format"
                                          SelectionChanged="RecordingFormatCombo_Changed">
                                    <ComboBoxItem Content="GIF"
                                                  ToolTip="Save recordings as GIF animations"
                                                  AutomationProperties.Name="GIF recording format"
                                                  AutomationProperties.HelpText="Save recordings as animated GIF files."/>
                                    <ComboBoxItem Content="MP4"
                                                  ToolTip="Save recordings as MP4 videos"
                                                  AutomationProperties.Name="MP4 recording format"
                                                  AutomationProperties.HelpText="Save recordings as MP4 video files."/>
                                    <ComboBoxItem Content="WebM"
                                                  ToolTip="Save recordings as WebM videos"
                                                  AutomationProperties.Name="WebM recording format"
                                                  AutomationProperties.HelpText="Save recordings as WebM video files."/>
                                    <ComboBoxItem Content="MKV"
                                                  ToolTip="Save recordings as MKV videos"
                                                  AutomationProperties.Name="MKV recording format"
                                                  AutomationProperties.HelpText="Save recordings as MKV video files."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE9D2;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Resolution" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Limit recording size. Original keeps the selected area's pixels." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="RecordingQualityCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          ToolTip="Limit recording size. Original keeps the sharpest output."
                                          AutomationProperties.Name="Recording resolution"
                                          SelectionChanged="RecordingQualityCombo_Changed">
                                    <ComboBoxItem Content="Original (sharpest)"
                                                  ToolTip="Keep the selected area's original pixel size"
                                                  AutomationProperties.Name="Original recording resolution"
                                                  AutomationProperties.HelpText="Record at the selected area's original pixel size for the sharpest output."/>
                                    <ComboBoxItem Content="1080p max"
                                                  ToolTip="Limit recordings to 1080p"
                                                  AutomationProperties.Name="1080p recording resolution"
                                                  AutomationProperties.HelpText="Scale recordings down to 1080p when needed."/>
                                    <ComboBoxItem Content="720p max"
                                                  ToolTip="Limit recordings to 720p"
                                                  AutomationProperties.Name="720p recording resolution"
                                                  AutomationProperties.HelpText="Scale recordings down to 720p when needed."/>
                                    <ComboBoxItem Content="480p max"
                                                  ToolTip="Limit recordings to 480p"
                                                  AutomationProperties.Name="480p recording resolution"
                                                  AutomationProperties.HelpText="Scale recordings down to 480p when needed."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE916;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="FPS" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Control how many frames are captured each second." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="RecordingFpsCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          ToolTip="Choose how many frames are captured each second"
                                          AutomationProperties.Name="Recording FPS"
                                          SelectionChanged="RecordingFpsCombo_Changed">
                                    <ComboBoxItem Content="15" Tag="15"
                                                  ToolTip="Capture 15 frames per second"
                                                  AutomationProperties.Name="15 FPS"
                                                  AutomationProperties.HelpText="Capture smoother-than-low-power recordings at 15 frames per second."/>
                                    <ComboBoxItem Content="24" Tag="24"
                                                  ToolTip="Capture 24 frames per second"
                                                  AutomationProperties.Name="24 FPS"
                                                  AutomationProperties.HelpText="Capture video-like motion at 24 frames per second."/>
                                    <ComboBoxItem Content="30" Tag="30"
                                                  ToolTip="Capture 30 frames per second for slower screens or lower CPU use"
                                                  AutomationProperties.Name="30 FPS"
                                                  AutomationProperties.HelpText="Capture at 30 frames per second for slower screens or lower CPU use."/>
                                    <ComboBoxItem Content="60" Tag="60"
                                                  ToolTip="Capture 60 frames per second by default for smooth video"
                                                  AutomationProperties.Name="60 FPS"
                                                  AutomationProperties.HelpText="Capture smooth video at 60 frames per second."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE7B3;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Show cursor in recordings" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Include pointer movement in recorded output." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="RecordShowCursorCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Show cursor in recordings"
                                          ToolTip="Include pointer movement in recorded output."
                                          Checked="RecordShowCursorCheck_Changed" Unchecked="RecordShowCursorCheck_Changed"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="RecordingPreferenceStatusText"
                               Style="{StaticResource SettingsStatusText}"
                               Visibility="Collapsed"
                               Margin="4,8,0,0"/>

                    <TextBlock x:Name="AudioSettingsLabel" Text="Audio" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border x:Name="VideoOnlySettings" Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE720;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Record microphone" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Capture audio from the selected input device." Style="{StaticResource SettingDescription}"/>
                                    <ComboBox x:Name="MicDeviceCombo" Margin="0,8,0,0"
                                              ToolTip="Choose the microphone input device"
                                              AutomationProperties.Name="Microphone input device"
                                              SelectionChanged="MicDeviceCombo_Changed"/>
                                </StackPanel>
                                <CheckBox x:Name="RecordMicCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Record microphone"
                                          ToolTip="Capture audio from the selected input device."
                                          Checked="RecordMicCheck_Changed" Unchecked="RecordMicCheck_Changed"/>
                            </Grid>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>

                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE767;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Record desktop audio" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Capture system audio from the selected output device." Style="{StaticResource SettingDescription}"/>
                                    <ComboBox x:Name="DesktopAudioDeviceCombo" Margin="0,8,0,0"
                                              ToolTip="Choose the desktop audio output device"
                                              AutomationProperties.Name="Desktop audio output device"
                                              SelectionChanged="DesktopAudioDeviceCombo_Changed"/>
                                </StackPanel>
                                <CheckBox x:Name="RecordDesktopAudioCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Record desktop audio"
                                          ToolTip="Capture system audio from the selected output device."
                                          Checked="RecordDesktopAudioCheck_Changed" Unchecked="RecordDesktopAudioCheck_Changed"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- OCR & Translation -->
            <ScrollViewer x:Name="OcrPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="26,58,26,22">
                <StackPanel>
                    <TextBlock Text="Recognition" Style="{StaticResource SectionLabel}"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Style="{StaticResource SettingIconFrame}">
                                <TextBlock Text="&#xE8B7;" Style="{StaticResource SettingIconGlyph}"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="OCR language" Style="{StaticResource SettingTitle}"/>
                                <TextBlock x:Name="OcrLanguageStatusText" Style="{StaticResource SettingDescription}"
                                           ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                           AutomationProperties.Name="OCR language availability"
                                           AutomationProperties.LiveSetting="Polite"/>
                            </StackPanel>
                            <ComboBox x:Name="OcrLanguageCombo" Grid.Column="2" MinWidth="145"
                                      VerticalAlignment="Center"
                                      AutomationProperties.Name="OCR language"
                                      ToolTip="Choose the language used for text recognition."
                                      IsEditable="True" IsTextSearchEnabled="False"
                                      PreviewTextInput="OcrCombo_PreviewTextInput" PreviewKeyDown="OcrCombo_PreviewKeyDown"
                                      SelectionChanged="OcrLanguageCombo_SelectionChanged"/>
                        </Grid>
                        <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Style="{StaticResource SettingIconFrame}">
                                <TextBlock Text="&#xE8C8;" Style="{StaticResource SettingIconGlyph}"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Auto-copy OCR" Style="{StaticResource SettingTitle}"/>
                                <TextBlock Text="Copy recognized text without opening the result window." Style="{StaticResource SettingDescription}"/>
                            </StackPanel>
                            <CheckBox x:Name="OcrAutoCopyCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                      AutomationProperties.Name="Auto-copy OCR text"
                                      ToolTip="Copy recognized text without opening the result window."
                                      Checked="OcrAutoCopyCheck_Changed" Unchecked="OcrAutoCopyCheck_Changed"/>
                        </Grid>
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="OcrPreferenceStatusText"
                               Style="{StaticResource SettingsStatusText}"
                               Visibility="Collapsed"
                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                               AutomationProperties.Name="OCR preference status"
                               AutomationProperties.LiveSetting="Polite"
                               Margin="4,8,0,0"/>

                    <TextBlock Text="Translation" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8C1;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Default source language" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Language to translate from by default." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="TranslateFromCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Default source language"
                                          ToolTip="Language to translate from by default."
                                          IsEditable="True" IsTextSearchEnabled="False"
                                          PreviewTextInput="OcrCombo_PreviewTextInput" PreviewKeyDown="OcrCombo_PreviewKeyDown"
                                          SelectionChanged="TranslateFromCombo_Changed"/>
                            </Grid>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>

                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8C1;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Default target language" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Language to translate into by default." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="TranslateToCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Default target language"
                                          ToolTip="Language to translate into by default."
                                          IsEditable="True" IsTextSearchEnabled="False"
                                          PreviewTextInput="OcrCombo_PreviewTextInput" PreviewKeyDown="OcrCombo_PreviewKeyDown"
                                          SelectionChanged="TranslateToCombo_Changed"/>
                            </Grid>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>

                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE950;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Translation engine" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose which translator handles OCR text." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="TranslateModelCombo" Grid.Column="2" MinWidth="145"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Translation engine"
                                          ToolTip="Choose which translator handles OCR text."
                                          SelectionChanged="TranslateModelCombo_Changed">
                                    <ComboBoxItem Content="Open-source Local" Tag="2"
                                                  ToolTip="Use the fully local open-source translator."
                                                  AutomationProperties.Name="Open-source local translator"
                                                  AutomationProperties.HelpText="Translate OCR text locally without a cloud provider."/>
                                    <ComboBoxItem Content="Argos Translate" Tag="0"
                                                  ToolTip="Use the Argos local fallback translator."
                                                  AutomationProperties.Name="Argos Translate"
                                                  AutomationProperties.HelpText="Translate OCR text with the local Argos runtime."/>
                                    <ComboBoxItem Content="Google Translate" Tag="1"
                                                  ToolTip="Use Google Translate with an API key."
                                                  AutomationProperties.Name="Google Translate"
                                                  AutomationProperties.HelpText="Translate OCR text through Google using the configured API key."/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="TranslationPreferenceStatusText"
                               Style="{StaticResource SettingsStatusText}"
                               Visibility="Collapsed"
                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                               AutomationProperties.Name="Translation preference status"
                               AutomationProperties.LiveSetting="Polite"
                               Margin="4,8,0,0"/>

                    <TextBlock Text="Engines" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE950;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource SettingTitle}">
                                        <Run Text="Open-source Local"/>
                                        <Run Text=" — fully local" FontSize="11" Foreground="{DynamicResource ThemeMutedBrush}"/>
                                    </TextBlock>
                                    <TextBlock x:Name="OpenSourceLocalStatusText" Text="Checking install state..." Style="{StaticResource SettingDescription}"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Open-source local translation status"
                                               AutomationProperties.LiveSetting="Polite"/>
                                    <ProgressBar x:Name="OpenSourceLocalProgressBar" Height="3" Margin="0,4,0,0"
                                                 IsIndeterminate="True" Visibility="Collapsed"/>
                                </StackPanel>
                                <Button x:Name="OpenSourceLocalInstallBtn" Grid.Column="2" Content="Install"
                                         VerticalAlignment="Center"
                                         AutomationProperties.Name="Install open-source local translator"
                                         ToolTip="Install or remove the open-source local translation runtime"
                                         Cursor="Hand"
                                         Click="OpenSourceLocalInstallBtn_Click"/>
                            </Grid>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>

                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE950;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Style="{StaticResource SettingTitle}">
                                        <Hyperlink NavigateUri="https://github.com/argosopentech/argos-translate" RequestNavigate="Hyperlink_Navigate"
                                                   Foreground="{DynamicResource ThemeAccentBrush}" FontSize="13">Argos Translate</Hyperlink>
                                        <Run Text=" — local fallback engine" FontSize="11" Foreground="{DynamicResource ThemeMutedBrush}"/>
                                    </TextBlock>
                                    <TextBlock x:Name="ArgosStatusText" Text="Checking install state..." Style="{StaticResource SettingDescription}"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Argos translation status"
                                               AutomationProperties.LiveSetting="Polite"/>
                                    <ProgressBar x:Name="ArgosProgressBar" Height="3" Margin="0,4,0,0"
                                                 IsIndeterminate="True" Visibility="Collapsed"/>
                                </StackPanel>
                                <Button x:Name="ArgosInstallBtn" Grid.Column="2" Content="Install"
                                         VerticalAlignment="Center"
                                         AutomationProperties.Name="Install Argos Translate"
                                         ToolTip="Install or remove the Argos local translation runtime"
                                         Cursor="Hand"
                                         Click="ArgosInstallBtn_Click"/>
                            </Grid>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>

                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE72E;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Google Translate API key" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Optional cloud translation key." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <PasswordBox x:Name="GoogleApiKeyBox" Grid.Column="2" MinWidth="160"
                                         VerticalAlignment="Center"
                                         AutomationProperties.Name="Google Translate API key"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}"
                                         Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                         BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                         PasswordChanged="GoogleApiKeyBox_Changed"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="SettingsPanel" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="26,58,26,22">
                <StackPanel>
                    <TextBlock Text="Language" Style="{StaticResource SectionLabel}"/>
                    <Border Style="{StaticResource Card}">
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Style="{StaticResource SettingIconFrame}">
                                <TextBlock Text="&#xF2B7;" Style="{StaticResource SettingIconGlyph}"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Interface language" Style="{StaticResource SettingTitle}"/>
                                <TextBlock Text="Choose your preferred language for OddSnap." Style="{StaticResource SettingDescription}"/>
                            </StackPanel>
                            <ComboBox x:Name="InterfaceLanguageCombo" Grid.Column="2" MinWidth="145"
                                      VerticalAlignment="Center"
                                      AutomationProperties.Name="Interface language"
                                      ToolTip="Choose your preferred language for OddSnap."
                                      SelectionChanged="InterfaceLanguageCombo_SelectionChanged"/>
                        </Grid>
                    </Border>

                    <TextBlock Text="Sound &amp; Motion" Style="{StaticResource SectionLabel}"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE7F5;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Mute capture sounds" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Silence the sound played when capturing." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="MuteSoundsCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Mute capture sounds"
                                          ToolTip="Silence the sound played when capturing."
                                          Checked="MuteSoundsCheck_Changed" Unchecked="MuteSoundsCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE7C1;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Disable animations" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Reduce motion effects across the app." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="DisableAnimationsCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Disable animations"
                                          ToolTip="Reduce motion effects across the app."
                                          Checked="DisableAnimationsCheck_Changed" Unchecked="DisableAnimationsCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE71E;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="UI scale" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Manually scale OddSnap windows, toasts, and capture controls." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="UiScaleCombo" Grid.Column="2" MinWidth="110"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="UI scale"
                                          ToolTip="Manually scale OddSnap windows, toasts, and capture controls."
                                          SelectionChanged="UiScaleCombo_SelectionChanged">
                                    <ComboBoxItem Content="80%" Tag="0.8"
                                                  ToolTip="Scale OddSnap UI to 80%."
                                                  AutomationProperties.Name="80 percent UI scale"
                                                  AutomationProperties.HelpText="Make OddSnap windows, toasts, and capture controls smaller."/>
                                    <ComboBoxItem Content="90%" Tag="0.9"
                                                  ToolTip="Scale OddSnap UI to 90%."
                                                  AutomationProperties.Name="90 percent UI scale"
                                                  AutomationProperties.HelpText="Make OddSnap windows, toasts, and capture controls slightly smaller."/>
                                    <ComboBoxItem Content="100%" Tag="1.0"
                                                  ToolTip="Use normal OddSnap UI scale."
                                                  AutomationProperties.Name="100 percent UI scale"
                                                  AutomationProperties.HelpText="Use the default OddSnap window, toast, and capture-control size."/>
                                    <ComboBoxItem Content="110%" Tag="1.1"
                                                  ToolTip="Scale OddSnap UI to 110%."
                                                  AutomationProperties.Name="110 percent UI scale"
                                                  AutomationProperties.HelpText="Make OddSnap windows, toasts, and capture controls slightly larger."/>
                                    <ComboBoxItem Content="120%" Tag="1.2"
                                                  ToolTip="Scale OddSnap UI to 120%."
                                                  AutomationProperties.Name="120 percent UI scale"
                                                  AutomationProperties.HelpText="Make OddSnap windows, toasts, and capture controls larger."/>
                                    <ComboBoxItem Content="130%" Tag="1.3"
                                                  ToolTip="Scale OddSnap UI to 130%."
                                                  AutomationProperties.Name="130 percent UI scale"
                                                  AutomationProperties.HelpText="Make OddSnap windows, toasts, and capture controls much larger."/>
                                    <ComboBoxItem Content="140%" Tag="1.4"
                                                  ToolTip="Scale OddSnap UI to 140%."
                                                  AutomationProperties.Name="140 percent UI scale"
                                                  AutomationProperties.HelpText="Make OddSnap windows, toasts, and capture controls extra large."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE767;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Sound pack" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose the sound pack for notifications." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="SoundPackCombo" Grid.Column="2" MinWidth="145"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Sound pack"
                                          ToolTip="Choose the sound pack for notifications."
                                          SelectionChanged="SoundPackCombo_SelectionChanged">
                                    <ComboBoxItem Content="Default"
                                                  ToolTip="Use the default notification sounds."
                                                  AutomationProperties.Name="Default sound pack"
                                                  AutomationProperties.HelpText="Use OddSnap's standard notification sounds."/>
                                    <ComboBoxItem Content="Soft"
                                                  ToolTip="Use quieter notification sounds."
                                                  AutomationProperties.Name="Soft sound pack"
                                                  AutomationProperties.HelpText="Use softer notification sounds for captures and previews."/>
                                    <ComboBoxItem Content="Retro"
                                                  ToolTip="Use retro notification sounds."
                                                  AutomationProperties.Name="Retro sound pack"
                                                  AutomationProperties.HelpText="Use retro-style notification sounds for captures and previews."/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="GeneralPreferenceStatusText"
                               Style="{StaticResource SettingsStatusText}"
                               Visibility="Collapsed"
                               Margin="4,8,0,0"/>

                    <TextBlock Text="Tray" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Style="{StaticResource SettingIconFrame}">
                                <TextBlock Text="&#xE7F4;" Style="{StaticResource SettingIconGlyph}"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Tray icon left click" Style="{StaticResource SettingTitle}"/>
                                <TextBlock Text="Choose what happens when you click the OddSnap tray icon." Style="{StaticResource SettingDescription}"/>
                            </StackPanel>
                            <ComboBox x:Name="TrayLeftClickActionCombo" Grid.Column="2" MinWidth="170"
                                      VerticalAlignment="Center"
                                      AutomationProperties.Name="Tray icon left click action"
                                      ToolTip="Choose what happens when you click the OddSnap tray icon."
                                      SelectionChanged="TrayLeftClickActionCombo_SelectionChanged">
                                <ComboBoxItem Content="Area capture"/>
                                <ComboBoxItem Content="History"/>
                                <ComboBoxItem Content="Full screen capture"/>
                                <ComboBoxItem Content="Record"/>
                                <ComboBoxItem Content="Scroll capture"/>
                                <ComboBoxItem Content="Settings"/>
                                <ComboBoxItem Content="Open menu"/>
                                <ComboBoxItem Content="Do nothing"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <TextBlock Text="Search" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border x:Name="GeneralImageSearchSettingsCard" Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE721;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Show image search bar" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Display the search bar in the image browser." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="ShowImageSearchBarCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Show image search bar"
                                          ToolTip="Display the search bar in the image browser."
                                          Checked="ShowImageSearchBarCheck_Changed" Unchecked="ShowImageSearchBarCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8B7;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Auto-index images" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Automatically index new images for search." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="AutoIndexImagesCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Auto-index images"
                                          ToolTip="Automatically index new images for search."
                                          Checked="AutoIndexImagesCheck_Changed" Unchecked="AutoIndexImagesCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE946;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Show search diagnostics" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Show diagnostics and performance info in search." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="ShowImageSearchDiagnosticsCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Show search diagnostics"
                                          ToolTip="Show diagnostics and performance info in search."
                                          Checked="ShowImageSearchDiagnosticsCheck_Changed" Unchecked="ShowImageSearchDiagnosticsCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8FD;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Image index cache" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Manage or rebuild the search index cache." Style="{StaticResource SettingDescription}"/>
                                    <TextBlock x:Name="ImageIndexMaintenanceStatusText"
                                               Style="{StaticResource SettingsStatusText}"
                                               Visibility="Collapsed"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Image index status"
                                               AutomationProperties.LiveSetting="Polite"
                                               Margin="0,6,0,0"/>
                                </StackPanel>
                                <Button x:Name="ResetImageIndexesBtn" Grid.Column="2" Content="Reset cache"
                                        MinWidth="120" VerticalAlignment="Center"
                                        AutomationProperties.Name="Reset image search cache"
                                        ToolTip="Reset the image search index cache"
                                        Cursor="Hand" Click="ResetImageIndexesBtn_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="ToastPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="26,58,26,22"
                          PreviewMouseMove="ToastPanel_PreviewMouseMove"
                          PreviewMouseLeftButtonUp="ToastPanel_PreviewMouseLeftButtonUp">
                <StackPanel>
                    <TextBlock Text="Placement" Style="{StaticResource SectionLabel}"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE81D;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Toast position" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose where screenshot previews appear." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="ToastPositionCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Toast position"
                                          ToolTip="Choose where screenshot previews appear."
                                          SelectionChanged="ToastPositionCombo_SelectionChanged">
                                    <ComboBoxItem Content="Right"
                                                  ToolTip="Show previews near the right edge."
                                                  AutomationProperties.Name="Right toast position"
                                                  AutomationProperties.HelpText="Place screenshot previews near the right edge of the screen."/>
                                    <ComboBoxItem Content="Left"
                                                  ToolTip="Show previews near the left edge."
                                                  AutomationProperties.Name="Left toast position"
                                                  AutomationProperties.HelpText="Place screenshot previews near the left edge of the screen."/>
                                    <ComboBoxItem Content="Top Left"
                                                  ToolTip="Show previews in the top-left corner."
                                                  AutomationProperties.Name="Top-left toast position"
                                                  AutomationProperties.HelpText="Place screenshot previews in the top-left corner of the screen."/>
                                    <ComboBoxItem Content="Top Right"
                                                  ToolTip="Show previews in the top-right corner."
                                                  AutomationProperties.Name="Top-right toast position"
                                                  AutomationProperties.HelpText="Place screenshot previews in the top-right corner of the screen."/>
                                </ComboBox>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE916;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Toast duration" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Set how long previews stay visible." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="ToastDurationCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Toast duration"
                                          ToolTip="Set how long previews stay visible."
                                          SelectionChanged="ToastDurationCombo_SelectionChanged">
                                    <ComboBoxItem Content="1.5 seconds" Tag="1.5"
                                                  ToolTip="Hide previews after 1.5 seconds."
                                                  AutomationProperties.Name="1.5 second toast duration"
                                                  AutomationProperties.HelpText="Keep screenshot previews visible for 1.5 seconds."/>
                                    <ComboBoxItem Content="2 seconds" Tag="2"
                                                  ToolTip="Hide previews after 2 seconds."
                                                  AutomationProperties.Name="2 second toast duration"
                                                  AutomationProperties.HelpText="Keep screenshot previews visible for 2 seconds."/>
                                    <ComboBoxItem Content="2.5 seconds" Tag="2.5"
                                                  ToolTip="Hide previews after 2.5 seconds."
                                                  AutomationProperties.Name="2.5 second toast duration"
                                                  AutomationProperties.HelpText="Keep screenshot previews visible for 2.5 seconds."/>
                                    <ComboBoxItem Content="3 seconds" Tag="3"
                                                  ToolTip="Hide previews after 3 seconds."
                                                  AutomationProperties.Name="3 second toast duration"
                                                  AutomationProperties.HelpText="Keep screenshot previews visible for 3 seconds."/>
                                    <ComboBoxItem Content="4 seconds" Tag="4"
                                                  ToolTip="Hide previews after 4 seconds."
                                                  AutomationProperties.Name="4 second toast duration"
                                                  AutomationProperties.HelpText="Keep screenshot previews visible for 4 seconds."/>
                                    <ComboBoxItem Content="5 seconds" Tag="5"
                                                  ToolTip="Hide previews after 5 seconds."
                                                  AutomationProperties.Name="5 second toast duration"
                                                  AutomationProperties.HelpText="Keep screenshot previews visible for 5 seconds."/>
                                </ComboBox>
                            </Grid>
                            <TextBlock x:Name="ToastPreferenceStatusText"
                                       Style="{StaticResource SettingsStatusText}"
                                       Visibility="Collapsed"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                       AutomationProperties.Name="Toast preference status"
                                       AutomationProperties.HelpText="{Binding Text, RelativeSource={RelativeSource Self}}"
                                       AutomationProperties.LiveSetting="Polite"
                                       Margin="34,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Behavior" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE840;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Auto-pin screenshot previews" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Keep new previews open until dismissed." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="AutoPinPreviewsCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                      AutomationProperties.Name="Auto-pin screenshot previews"
                                      ToolTip="Keep new previews open until dismissed."
                                      Checked="AutoPinPreviewsCheck_Changed" Unchecked="AutoPinPreviewsCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE7C1;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Fade out instead of sliding away" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Use a quieter preview dismissal animation." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="ToastFadeOutCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                      AutomationProperties.Name="Fade out instead of sliding away"
                                      ToolTip="Use a quieter preview dismissal animation."
                                      Checked="ToastFadeOutCheck_Changed" Unchecked="ToastFadeOutCheck_Changed"/>
                            </Grid>
                            <Border x:Name="ToastFadeDurationSeparator" Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid x:Name="ToastFadeDurationRow" Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE916;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Fade-out duration" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Control how quickly faded previews disappear." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="ToastFadeDurationCombo" Grid.Column="2" MinWidth="150"
                                          VerticalAlignment="Center"
                                          AutomationProperties.Name="Fade-out duration"
                                          ToolTip="Control how quickly faded previews disappear."
                                          SelectionChanged="ToastFadeDurationCombo_SelectionChanged">
                                    <ComboBoxItem Content="1 second" Tag="1"
                                                  ToolTip="Fade previews out over 1 second."
                                                  AutomationProperties.Name="1 second fade-out duration"
                                                  AutomationProperties.HelpText="Dismiss screenshot previews with a 1 second fade-out animation."/>
                                    <ComboBoxItem Content="2 seconds" Tag="2"
                                                  ToolTip="Fade previews out over 2 seconds."
                                                  AutomationProperties.Name="2 second fade-out duration"
                                                  AutomationProperties.HelpText="Dismiss screenshot previews with a 2 second fade-out animation."/>
                                    <ComboBoxItem Content="3 seconds" Tag="3"
                                                  ToolTip="Fade previews out over 3 seconds."
                                                  AutomationProperties.Name="3 second fade-out duration"
                                                  AutomationProperties.HelpText="Dismiss screenshot previews with a 3 second fade-out animation."/>
                                    <ComboBoxItem Content="5 seconds" Tag="5"
                                                  ToolTip="Fade previews out over 5 seconds."
                                                  AutomationProperties.Name="5 second fade-out duration"
                                                  AutomationProperties.HelpText="Dismiss screenshot previews with a 5 second fade-out animation."/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Overlay Buttons" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Drag buttons to move them. You can also select one, then click a slot or another button." FontSize="13" TextWrapping="Wrap"/>
                                    <TextBlock x:Name="ToastLayoutSelectionText" Text="Selected: Close" Style="{StaticResource InfoText}" Margin="0,4,0,0"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Toast layout selection"
                                               AutomationProperties.HelpText="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.LiveSetting="Polite"/>
                                </StackPanel>
                                <Button x:Name="ResetToastButtonsBtn" Grid.Column="1" Content="Reset Layout"
                                        Margin="8,0,0,0"
                                        AutomationProperties.Name="Reset toast button layout"
                                        ToolTip="Restore the default toast button layout"
                                        Cursor="Hand" Click="ResetToastButtonsBtn_Click"/>
                            </Grid>

                            <Border Margin="0,14,0,0"
                                    CornerRadius="12"
                                    BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                                    BorderThickness="1"
                                    Background="{DynamicResource ThemeInputBackgroundBrush}"
                                    Padding="16">
                                <Grid x:Name="ToastLayoutSurfaceRoot">
                                    <StackPanel x:Name="ToastLayoutStack" Width="300" HorizontalAlignment="Center">
                                    <Border Width="300" Height="236" CornerRadius="18"
                                            Background="#12000000"
                                            BorderBrush="{DynamicResource ThemeWindowBorderBrush}"
                                            BorderThickness="1">
                                        <Grid x:Name="ToastLayoutPreviewSurface">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="4"/>
                                            </Grid.RowDefinitions>

                                            <Border Grid.Row="0" Margin="12,12,12,0" CornerRadius="14,14,10,10" Background="#D35A93E6">
                                                <Grid>
                                                    <Border x:Name="ToastSlotTopLeft"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            HorizontalAlignment="Left" VerticalAlignment="Top"
                                                            Margin="8,8,0,0"
                                                            Background="Transparent" BorderBrush="#2DFFFFFF" BorderThickness="1"
                                                            Cursor="Hand"
                                                            ToolTip="Move selected toast button to top-left"
                                                            AutomationProperties.Name="top-left toast slot"
                                                            AutomationProperties.HelpText="Press Enter or Space to place the selected toast button here."
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutSlot_MouseLeftButtonDown"
                                                            DragEnter="ToastLayoutSlot_DragEnter"
                                                            DragOver="ToastLayoutSlot_DragOver"
                                                            DragLeave="ToastLayoutSlot_DragLeave"
                                                            Drop="ToastLayoutSlot_Drop"
                                                            Tag="TopLeft"/>
                                                    <Border x:Name="ToastSlotTopInnerLeft"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            HorizontalAlignment="Left" VerticalAlignment="Top"
                                                            Margin="40,6,0,0"
                                                            Background="Transparent" BorderBrush="#2DFFFFFF" BorderThickness="1"
                                                            Cursor="Hand"
                                                            ToolTip="Move selected toast button to top inner-left"
                                                            AutomationProperties.Name="top inner-left toast slot"
                                                            AutomationProperties.HelpText="Press Enter or Space to place the selected toast button here."
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutSlot_MouseLeftButtonDown"
                                                            DragEnter="ToastLayoutSlot_DragEnter"
                                                            DragOver="ToastLayoutSlot_DragOver"
                                                            DragLeave="ToastLayoutSlot_DragLeave"
                                                            Drop="ToastLayoutSlot_Drop"
                                                            Tag="TopInnerLeft"/>
                                                    <Border x:Name="ToastSlotTopInnerRight"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            HorizontalAlignment="Right" VerticalAlignment="Top"
                                                            Margin="0,8,48,0"
                                                            Background="Transparent" BorderBrush="#2DFFFFFF" BorderThickness="1"
                                                            Cursor="Hand"
                                                            ToolTip="Move selected toast button to top inner-right"
                                                            AutomationProperties.Name="top inner-right toast slot"
                                                            AutomationProperties.HelpText="Press Enter or Space to place the selected toast button here."
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutSlot_MouseLeftButtonDown"
                                                            DragEnter="ToastLayoutSlot_DragEnter"
                                                            DragOver="ToastLayoutSlot_DragOver"
                                                            DragLeave="ToastLayoutSlot_DragLeave"
                                                            Drop="ToastLayoutSlot_Drop"
                                                            Tag="TopInnerRight"/>
                                                    <Border x:Name="ToastSlotTopRight"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            HorizontalAlignment="Right" VerticalAlignment="Top"
                                                            Margin="0,8,8,0"
                                                            Background="Transparent" BorderBrush="#2DFFFFFF" BorderThickness="1"
                                                            Cursor="Hand"
                                                            ToolTip="Move selected toast button to top-right"
                                                            AutomationProperties.Name="top-right toast slot"
                                                            AutomationProperties.HelpText="Press Enter or Space to place the selected toast button here."
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutSlot_MouseLeftButtonDown"
                                                            DragEnter="ToastLayoutSlot_DragEnter"
                                                            DragOver="ToastLayoutSlot_DragOver"
                                                            DragLeave="ToastLayoutSlot_DragLeave"
                                                            Drop="ToastLayoutSlot_Drop"
                                                            Tag="TopRight"/>
                                                    <Border x:Name="ToastSlotBottomLeft"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            HorizontalAlignment="Left" VerticalAlignment="Bottom"
                                                            Margin="8,0,0,8"
                                                            Background="Transparent" BorderBrush="#2DFFFFFF" BorderThickness="1"
                                                            Cursor="Hand"
                                                            ToolTip="Move selected toast button to bottom-left"
                                                            AutomationProperties.Name="bottom-left toast slot"
                                                            AutomationProperties.HelpText="Press Enter or Space to place the selected toast button here."
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutSlot_MouseLeftButtonDown"
                                                            DragEnter="ToastLayoutSlot_DragEnter"
                                                            DragOver="ToastLayoutSlot_DragOver"
                                                            DragLeave="ToastLayoutSlot_DragLeave"
                                                            Drop="ToastLayoutSlot_Drop"
                                                            Tag="BottomLeft"/>
                                                    <Border x:Name="ToastSlotBottomInnerLeft"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            HorizontalAlignment="Left" VerticalAlignment="Bottom"
                                                            Margin="40,0,0,6"
                                                            Background="Transparent" BorderBrush="#2DFFFFFF" BorderThickness="1"
                                                            Cursor="Hand"
                                                            ToolTip="Move selected toast button to bottom inner-left"
                                                            AutomationProperties.Name="bottom inner-left toast slot"
                                                            AutomationProperties.HelpText="Press Enter or Space to place the selected toast button here."
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutSlot_MouseLeftButtonDown"
                                                            DragEnter="ToastLayoutSlot_DragEnter"
                                                            DragOver="ToastLayoutSlot_DragOver"
                                                            DragLeave="ToastLayoutSlot_DragLeave"
                                                            Drop="ToastLayoutSlot_Drop"
                                                            Tag="BottomInnerLeft"/>
                                                    <Border x:Name="ToastSlotBottomInnerRight"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                                            Margin="0,0,48,8"
                                                            Background="Transparent" BorderBrush="#2DFFFFFF" BorderThickness="1"
                                                            Cursor="Hand"
                                                            ToolTip="Move selected toast button to bottom inner-right"
                                                            AutomationProperties.Name="bottom inner-right toast slot"
                                                            AutomationProperties.HelpText="Press Enter or Space to place the selected toast button here."
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutSlot_MouseLeftButtonDown"
                                                            DragEnter="ToastLayoutSlot_DragEnter"
                                                            DragOver="ToastLayoutSlot_DragOver"
                                                            DragLeave="ToastLayoutSlot_DragLeave"
                                                            Drop="ToastLayoutSlot_Drop"
                                                            Tag="BottomInnerRight"/>
                                                    <Border x:Name="ToastSlotBottomRight"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                                            Margin="0,0,8,8"
                                                            Background="Transparent" BorderBrush="#2DFFFFFF" BorderThickness="1"
                                                            Cursor="Hand"
                                                            ToolTip="Move selected toast button to bottom-right"
                                                            AutomationProperties.Name="bottom-right toast slot"
                                                            AutomationProperties.HelpText="Press Enter or Space to place the selected toast button here."
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutSlot_MouseLeftButtonDown"
                                                            DragEnter="ToastLayoutSlot_DragEnter"
                                                            DragOver="ToastLayoutSlot_DragOver"
                                                            DragLeave="ToastLayoutSlot_DragLeave"
                                                            Drop="ToastLayoutSlot_Drop"
                                                            Tag="BottomRight"/>

                                                    <Border x:Name="ToastLayoutCloseBtn"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                                            Cursor="Hand"
                                                            ToolTip="Move the close toast button"
                                                            AutomationProperties.Name="Close toast button"
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutButton_MouseLeftButtonDown"
                                                            MouseMove="ToastLayoutButton_MouseMove"
                                                            MouseLeftButtonUp="ToastLayoutButton_MouseLeftButtonUp"
                                                            DragEnter="ToastLayoutButton_DragEnter"
                                                            Drop="ToastLayoutButton_Drop"
                                                            Tag="Close">
                                                        <Image x:Name="ToastLayoutCloseIcon" Width="14" Height="14" Stretch="Uniform" Opacity="0.9"
                                                               RenderOptions.BitmapScalingMode="HighQuality"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border x:Name="ToastLayoutPinBtn"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                                            Cursor="Hand"
                                                            ToolTip="Move the pin toast button"
                                                            AutomationProperties.Name="Pin toast button"
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutButton_MouseLeftButtonDown"
                                                            MouseMove="ToastLayoutButton_MouseMove"
                                                            MouseLeftButtonUp="ToastLayoutButton_MouseLeftButtonUp"
                                                            DragEnter="ToastLayoutButton_DragEnter"
                                                            Drop="ToastLayoutButton_Drop"
                                                            Tag="Pin">
                                                        <Image x:Name="ToastLayoutPinIcon" Width="14" Height="14" Stretch="Uniform" Opacity="0.9"
                                                               RenderOptions.BitmapScalingMode="HighQuality"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border x:Name="ToastLayoutSaveBtn"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                                            Cursor="Hand"
                                                            ToolTip="Move the save toast button"
                                                            AutomationProperties.Name="Save toast button"
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutButton_MouseLeftButtonDown"
                                                            MouseMove="ToastLayoutButton_MouseMove"
                                                            MouseLeftButtonUp="ToastLayoutButton_MouseLeftButtonUp"
                                                            DragEnter="ToastLayoutButton_DragEnter"
                                                            Drop="ToastLayoutButton_Drop"
                                                            Tag="Save">
                                                        <Image x:Name="ToastLayoutSaveIcon" Width="14" Height="14" Stretch="Uniform" Opacity="0.9"
                                                               RenderOptions.BitmapScalingMode="HighQuality"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border x:Name="ToastLayoutOfficeBtn"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                                            Cursor="Hand"
                                                            ToolTip="Move the office export toast button"
                                                            AutomationProperties.Name="Office export toast button"
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutButton_MouseLeftButtonDown"
                                                            MouseMove="ToastLayoutButton_MouseMove"
                                                            MouseLeftButtonUp="ToastLayoutButton_MouseLeftButtonUp"
                                                            DragEnter="ToastLayoutButton_DragEnter"
                                                            Drop="ToastLayoutButton_Drop"
                                                            Tag="Office">
                                                        <Image x:Name="ToastLayoutOfficeIcon" Width="14" Height="14" Stretch="Uniform" Opacity="0.9"
                                                               RenderOptions.BitmapScalingMode="HighQuality"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border x:Name="ToastLayoutAiRedirectBtn"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                                            Cursor="Hand"
                                                            ToolTip="Move the AI redirect toast button"
                                                            AutomationProperties.Name="AI redirect toast button"
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutButton_MouseLeftButtonDown"
                                                            MouseMove="ToastLayoutButton_MouseMove"
                                                            MouseLeftButtonUp="ToastLayoutButton_MouseLeftButtonUp"
                                                            DragEnter="ToastLayoutButton_DragEnter"
                                                            Drop="ToastLayoutButton_Drop"
                                                            Tag="AiRedirect">
                                                        <Image x:Name="ToastLayoutAiRedirectIcon" Width="14" Height="14" Stretch="Uniform" Opacity="0.9"
                                                               RenderOptions.BitmapScalingMode="HighQuality"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <Border x:Name="ToastLayoutDeleteBtn"
                                                            Width="30" Height="30" CornerRadius="9"
                                                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0"
                                                            Cursor="Hand"
                                                            ToolTip="Move the delete toast button"
                                                            AutomationProperties.Name="Delete toast button"
                                                            AllowDrop="True"
                                                            MouseLeftButtonDown="ToastLayoutButton_MouseLeftButtonDown"
                                                            MouseMove="ToastLayoutButton_MouseMove"
                                                            MouseLeftButtonUp="ToastLayoutButton_MouseLeftButtonUp"
                                                            DragEnter="ToastLayoutButton_DragEnter"
                                                            Drop="ToastLayoutButton_Drop"
                                                            Tag="Delete">
                                                        <Image x:Name="ToastLayoutDeleteIcon" Width="14" Height="14" Stretch="Uniform" Opacity="0.9"
                                                               RenderOptions.BitmapScalingMode="HighQuality"
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </Grid>
                                            </Border>

                                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="14,10,14,8">
                                                <Border Width="30" Height="30" CornerRadius="9" Background="#12FFFFFF" Margin="0,0,12,0"/>
                                                <StackPanel>
                                                    <TextBlock Text="Screenshot saved" FontSize="12.5" FontWeight="SemiBold"
                                                               Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                                                    <TextBlock Text="Preview toast with overlay buttons." FontSize="11.5" Opacity="0.62"
                                                               Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                                                </StackPanel>
                                            </StackPanel>

                                            <Border Grid.Row="2" Margin="12,0,12,12" CornerRadius="2" Background="#65FFFFFF"/>
                                        </Grid>
                                    </Border>

                                    <Border Width="300" Margin="0,10,0,0"
                                            CornerRadius="14"
                                            Background="#10000000"
                                            BorderBrush="{DynamicResource ThemeWindowBorderBrush}"
                                            BorderThickness="1"
                                            Padding="12">
                                        <StackPanel>
                                            <TextBlock Text="Shelf" FontSize="12.5" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                                            <TextBlock Text="Hidden buttons live here. Drag one back out or select it, then click a slot." Style="{StaticResource InfoText}" Margin="0,4,0,10"/>

                                            <Border x:Name="ToastHiddenShelf"
                                                    CornerRadius="12"
                                                    Background="#12000000"
                                                    BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                                                    BorderThickness="1"
                                                    Padding="10"
                                                    MinHeight="68"
                                                    AllowDrop="True"
                                                    DragEnter="ToastShelf_DragEnter"
                                                    DragOver="ToastShelf_DragOver"
                                                    DragLeave="ToastShelf_DragLeave"
                                                    Drop="ToastShelf_Drop">
                                                <Grid>
                                                    <Border x:Name="ToastHiddenShelfDropCue"
                                                            CornerRadius="10"
                                                            Background="#34FFFFFF"
                                                            BorderBrush="#C8FFFFFF"
                                                            BorderThickness="1.8"
                                                            Visibility="Collapsed"
                                                            Margin="-2">
                                                        <TextBlock Text="Drop Here To Store"
                                                                   FontSize="11.5"
                                                                   FontWeight="SemiBold"
                                                                   Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                                                   HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center"/>
                                                    </Border>
                                                    <TextBlock x:Name="ToastHiddenShelfEmpty"
                                                               Text="No hidden buttons. Drag one here to store it."
                                                               Style="{StaticResource InfoText}"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"/>
                                                    <WrapPanel x:Name="ToastHiddenButtonsPanel"/>
                                                </Grid>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                    </StackPanel>

                                    <Canvas x:Name="ToastDragLayer" IsHitTestVisible="False" Panel.ZIndex="50">
                                        <Border x:Name="ToastDragGhost"
                                                Width="30" Height="30" CornerRadius="9"
                                                Background="#D0202020"
                                                BorderBrush="#C0FFFFFF"
                                                BorderThickness="1.6"
                                                Visibility="Collapsed"
                                                Opacity="0.92">
                                            <Viewbox Width="14" Height="14" Stretch="Uniform">
                                                <Grid x:Name="ToastDragGhostGlyphHost"/>
                                            </Viewbox>
                                        </Border>
                                    </Canvas>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- About -->
            <ScrollViewer x:Name="AboutPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="26,58,26,22">
                <StackPanel>
                    <TextBlock Text="Updates" Style="{StaticResource SectionLabel}"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE895;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Automatically check for updates" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Notify when a newer OddSnap release is available." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="AutoUpdateCheck" Grid.Column="2" Content="" FontSize="13" VerticalAlignment="Center"
                                          AutomationProperties.Name="Automatically check for updates"
                                          ToolTip="Notify when a newer OddSnap release is available."
                                          Checked="AutoUpdateCheck_Changed" Unchecked="AutoUpdateCheck_Changed"/>
                            </Grid>
                            <TextBlock x:Name="AboutPreferenceStatusText"
                                       Style="{StaticResource SettingsStatusText}"
                                       Visibility="Collapsed"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                       AutomationProperties.Name="Update preference status"
                                       AutomationProperties.LiveSetting="Polite"
                                       Margin="34,5,0,0"/>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE9D5;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock x:Name="UpdateStatusText" Text="Checking for updates..." Style="{StaticResource SettingTitle}"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Update status"
                                               AutomationProperties.LiveSetting="Polite"/>
                                    <TextBlock x:Name="UpdateDetailText" Text="" Style="{StaticResource SettingDescription}"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Update details"/>
                                </StackPanel>
                                <Button x:Name="CheckUpdatesButton" Grid.Column="2" Content="Check now" Margin="8,0,0,0"
                                        VerticalAlignment="Center" ToolTip="Check for updates"
                                        AutomationProperties.Name="Check for updates"
                                        Cursor="Hand" Click="CheckUpdatesButton_Click"/>
                                <Button x:Name="DownloadUpdateButton" Grid.Column="3" Content="Open release" Margin="6,0,0,0"
                                        VerticalAlignment="Center" Visibility="Collapsed" ToolTip="Open or install the latest release"
                                        AutomationProperties.Name="Open or install update"
                                        Cursor="Hand" Click="DownloadUpdateButton_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Support" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Optional ways to support ongoing work."
                                       FontSize="11.5" Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                                       TextWrapping="Wrap" Margin="0,0,0,14"/>

                            <Border x:Name="KoFiSupportAction" Style="{StaticResource AboutActionRow}"
                                    Focusable="True"
                                    AutomationProperties.Name="Open Ko-fi support link"
                                    MouseLeftButtonUp="KoFiSupport_Click"
                                    KeyDown="KoFiSupport_KeyDown">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Style="{StaticResource AboutIconFrame}">
                                        <Image Source="pack://application:,,,/Assets/kofi_logo.png"
                                               Width="20" Height="20" Stretch="Uniform"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Ko-fi" FontSize="12.5" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                                        <TextBlock Text="Support development with Ko-fi" FontSize="11"
                                                   Foreground="{DynamicResource ThemeTextSecondaryBrush}" Margin="0,1,8,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="Open" FontSize="11" Opacity="0.45"
                                               Foreground="{DynamicResource ThemeTextPrimaryBrush}" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>

                            <Border x:Name="PayPalSupportAction" Style="{StaticResource AboutActionRow}" Margin="0"
                                    Focusable="True"
                                    AutomationProperties.Name="Open PayPal support link"
                                    MouseLeftButtonUp="PayPalSupport_Click"
                                    KeyDown="PayPalSupport_KeyDown">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Style="{StaticResource AboutIconFrame}">
                                        <Viewbox Width="20" Height="20" Stretch="Uniform">
                                            <Canvas Width="52" Height="52">
                                                <Path Fill="#002991" Data="M38.914 13.35c0 5.574-5.144 12.15-12.927 12.15H18.49l-.368 2.322L16.373 39H7.056l5.605-36h15.095c5.083 0 9.082 2.833 10.555 6.77a9.687 9.687 0 0 1 .603 3.58z"/>
                                                <Path Fill="#60CDFF" Data="M44.284 23.7A12.894 12.894 0 0 1 31.53 34.5h-5.206L24.157 48H14.89l1.483-9 1.75-11.178.367-2.322h7.497c7.773 0 12.927-6.576 12.927-12.15 3.825 1.974 6.055 5.963 5.37 10.35z"/>
                                                <Path Fill="#008CFF" Data="M38.914 13.35C37.31 12.511 35.365 12 33.248 12h-12.64L18.49 25.5h7.497c7.773 0 12.927-6.576 12.927-12.15z"/>
                                            </Canvas>
                                        </Viewbox>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="PayPal" FontSize="12.5" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                                        <TextBlock Text="Send a one-time donation" FontSize="11"
                                                   Foreground="{DynamicResource ThemeTextSecondaryBrush}" Margin="0,1,8,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="Open" FontSize="11" Opacity="0.45"
                                               Foreground="{DynamicResource ThemeTextPrimaryBrush}" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,8,0,14"/>

                    <Grid Style="{StaticResource SettingRow}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Row="0" Style="{StaticResource SettingIconFrame}">
                            <TextBlock Text="&#xE7E8;" Style="{StaticResource SettingIconGlyph}"/>
                        </Border>
                        <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Launch on startup" Style="{StaticResource SettingTitle}"/>
                            <TextBlock Text="Start OddSnap automatically when Windows starts." Style="{StaticResource SettingDescription}"/>
                        </StackPanel>
                        <CheckBox x:Name="StartWithWindowsCheck" Grid.Row="0" Grid.Column="2" Content="" FontSize="13"
                                  AutomationProperties.Name="Launch on startup"
                                  ToolTip="Start OddSnap automatically when Windows starts."
                                  VerticalAlignment="Center"
                                  Checked="StartWithWindowsCheck_Changed" Unchecked="StartWithWindowsCheck_Changed"/>

                        <Border Grid.Row="1" Style="{StaticResource SettingIconFrame}" Margin="0,12,0,0">
                            <TextBlock Text="&#xE8A5;" Style="{StaticResource SettingIconGlyph}"/>
                        </Border>
                        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,12,0,0">
                            <TextBlock Text="Create Start Menu shortcut" Style="{StaticResource SettingTitle}"/>
                            <TextBlock Text="Keep OddSnap in the current user's Start menu." Style="{StaticResource SettingDescription}"/>
                        </StackPanel>
                        <CheckBox x:Name="CreateStartMenuShortcutCheck" Grid.Row="1" Grid.Column="2" Content="" FontSize="13"
                                  AutomationProperties.Name="Create Start Menu shortcut"
                                  ToolTip="Keep OddSnap in the current user's Start menu."
                                  VerticalAlignment="Center" Margin="0,12,0,0"
                                  Checked="CreateStartMenuShortcutCheck_Changed" Unchecked="CreateStartMenuShortcutCheck_Changed"/>

                        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Margin="0,14,0,0">
                            <TextBlock x:Name="StartupPreferenceStatusText" Style="{StaticResource SettingsStatusText}"
                                       Visibility="Collapsed"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                       AutomationProperties.Name="Startup preference status"
                                       AutomationProperties.LiveSetting="Polite"
                                       Margin="34,0,0,8"/>
                            <WrapPanel Orientation="Horizontal">
                                <Button x:Name="ExportSettingsBtn" Content="Export settings" MinWidth="96"
                                        Margin="0,0,10,8" ToolTip="Export redacted settings to a JSON file"
                                        AutomationProperties.Name="Export settings"
                                        Cursor="Hand" Click="ExportSettingsButton_Click"/>
                                <Button x:Name="ImportSettingsBtn" Content="Import settings" MinWidth="96"
                                        Margin="0,0,10,8" ToolTip="Import settings from a JSON file"
                                        AutomationProperties.Name="Import settings"
                                        Cursor="Hand" Click="ImportSettingsButton_Click"/>
                            </WrapPanel>
                            <TextBlock x:Name="SettingsImportExportStatusText" Style="{StaticResource SettingsHelperText}"
                                       Visibility="Collapsed"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                       AutomationProperties.Name="Settings import and export status"
                                       AutomationProperties.LiveSetting="Polite"
                                       Margin="0,0,0,10"/>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,4,0,12"/>
                            <WrapPanel Orientation="Horizontal">
                                <Button x:Name="ResetSettingsBtn" Content="Reset Settings" Style="{StaticResource DangerButton}"
                                        ToolTip="Reset all settings to defaults"
                                        AutomationProperties.Name="Reset settings"
                                        Cursor="Hand" Click="ResetSettingsButton_Click" Margin="0,0,10,8"/>
                                <Button x:Name="UninstallOddSnapBtn" Content="Uninstall OddSnap" Style="{StaticResource DangerButton}"
                                        ToolTip="Start the OddSnap uninstall flow"
                                        AutomationProperties.Name="Uninstall OddSnap"
                                        Cursor="Hand" Click="UninstallButton_Click" Margin="0,0,10,8"/>
                            </WrapPanel>
                        </StackPanel>
                    </Grid>

                    <TextBlock x:Name="VersionText" FontSize="10.5" Opacity="0.28" HorizontalAlignment="Center" Margin="0,18,0,4"/>

                    <TextBlock Text="Links" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Margin="0,0,0,7">
                                <Hyperlink NavigateUri="https://github.com/maketeamake/oddsnap-custom" RequestNavigate="Hyperlink_Navigate"
                                           Style="{StaticResource AboutActionLink}" FontSize="12">Source Code</Hyperlink>
                                <Run Text=" — GitHub repository" FontSize="11" Foreground="{DynamicResource ThemeTextSecondaryBrush}"/>
                            </TextBlock>
                            <TextBlock Margin="0,0,0,7">
                                <Hyperlink NavigateUri="https://github.com/maketeamake/oddsnap-custom/releases/latest" RequestNavigate="Hyperlink_Navigate"
                                           Style="{StaticResource AboutActionLink}" FontSize="12">Releases</Hyperlink>
                                <Run Text=" — Download the latest version" FontSize="11" Foreground="{DynamicResource ThemeTextSecondaryBrush}"/>
                            </TextBlock>
                            <TextBlock Margin="0,0,0,7">
                                <Hyperlink NavigateUri="https://github.com/maketeamake/oddsnap-custom/issues" RequestNavigate="Hyperlink_Navigate"
                                           Style="{StaticResource AboutActionLink}" FontSize="12">Issues</Hyperlink>
                                <Run Text=" — Report bugs or request features" FontSize="11" Foreground="{DynamicResource ThemeTextSecondaryBrush}"/>
                            </TextBlock>
                            <TextBlock>
                                <Hyperlink NavigateUri="https://github.com/maketeamake/oddsnap-custom/blob/main/LICENSE" RequestNavigate="Hyperlink_Navigate"
                                           Style="{StaticResource AboutActionLink}" FontSize="12">License</Hyperlink>
                                <Run Text=" — MIT" FontSize="11" Foreground="{DynamicResource ThemeTextSecondaryBrush}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- History (merged: Screenshots + Text sub-tabs) -->
            <Grid x:Name="HistoryPanel" Visibility="Collapsed" Margin="26,58,26,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Category dropdown + actions -->
                <Border Grid.Row="0" Padding="0,0,0,6">
                    <StackPanel>
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="SelectBtn" Content="Select" Style="{StaticResource HistoryActionButton}"
                                         Margin="0,0,6,0" ToolTip="Select history items"
                                         AutomationProperties.Name="Select history items"
                                         AutomationProperties.HelpText="Select history items"
                                         Cursor="Hand" Click="ToggleSelectMode"/>
                                <Button x:Name="DeleteAllBtn" Content="Clear all" Style="{StaticResource HistoryDangerButton}"
                                        ToolTip="Delete all items in the current history tab"
                                        AutomationProperties.Name="Clear current history tab"
                                        AutomationProperties.HelpText="Delete all items in the current history tab"
                                        Cursor="Hand" Click="DeleteAllClick"/>
                                <Button x:Name="DeleteSelectedBtn" Content="Delete selected" Style="{StaticResource HistoryDangerButton}"
                                         Margin="6,0,0,0" Visibility="Collapsed"
                                         ToolTip="Delete selected history items"
                                         AutomationProperties.Name="Delete selected history items"
                                         AutomationProperties.HelpText="Delete selected history items"
                                         Cursor="Hand" Click="DeleteSelectedClick"/>
                            </StackPanel>
                            <ComboBox x:Name="HistoryCategoryCombo"
                                      MinWidth="130" Margin="0,0,8,0" VerticalAlignment="Center"
                                      ToolTip="Choose the history category"
                                      AutomationProperties.Name="History category"
                                      SelectionChanged="HistoryCategoryCombo_Changed"
                                      PreviewMouseLeftButtonDown="HistoryCategoryCombo_PreviewMouseLeftButtonDown">
                                <ComboBoxItem Content="Images" IsSelected="True"
                                              ToolTip="Show screenshot history"
                                              AutomationProperties.Name="Image history"
                                              AutomationProperties.HelpText="Show saved screenshot captures."/>
                                <ComboBoxItem Content="Text"
                                              ToolTip="Show OCR text history"
                                              AutomationProperties.Name="Text history"
                                              AutomationProperties.HelpText="Show saved OCR text captures."/>
                                <ComboBoxItem Content="Videos/GIFs"
                                              ToolTip="Show video and GIF history"
                                              AutomationProperties.Name="Video and GIF history"
                                              AutomationProperties.HelpText="Show saved video recordings and GIF captures."/>
                                <ComboBoxItem Content="Colors"
                                              ToolTip="Show color history"
                                              AutomationProperties.Name="Color history"
                                              AutomationProperties.HelpText="Show saved color picks."/>
                                <ComboBoxItem Content="Stickers"
                                              ToolTip="Show sticker history"
                                              AutomationProperties.Name="Sticker history"
                                              AutomationProperties.HelpText="Show saved sticker captures."/>
                                <ComboBoxItem Content="Codes"
                                              ToolTip="Show QR and barcode history"
                                              AutomationProperties.Name="Code history"
                                              AutomationProperties.HelpText="Show saved QR and barcode scans."/>
                            </ComboBox>
                            <ComboBox x:Name="HistoryUploadFilterCombo"
                                      MinWidth="128" Margin="0,0,8,0" VerticalAlignment="Center"
                                      ToolTip="Filter history by upload state"
                                      AutomationProperties.Name="Upload state filter"
                                      SelectionChanged="HistoryUploadFilterCombo_Changed">
                                <ComboBoxItem Content="All uploads" Tag="All" IsSelected="True"
                                              ToolTip="Show all upload states"
                                              AutomationProperties.Name="All uploads"
                                              AutomationProperties.HelpText="Show uploaded, not uploaded, and failed history items."/>
                                <ComboBoxItem Content="Uploaded" Tag="Uploaded"
                                              ToolTip="Show uploaded history items"
                                              AutomationProperties.Name="Uploaded history items"
                                              AutomationProperties.HelpText="Show items with a successful upload URL."/>
                                <ComboBoxItem Content="Not uploaded" Tag="NotUploaded"
                                              ToolTip="Show history items that have not uploaded"
                                              AutomationProperties.Name="Not uploaded history items"
                                              AutomationProperties.HelpText="Show items without an upload URL or upload error."/>
                                <ComboBoxItem Content="Failed" Tag="Failed"
                                              ToolTip="Show history items with upload errors"
                                              AutomationProperties.Name="Failed upload history items"
                                              AutomationProperties.HelpText="Show items with an upload error."/>
                            </ComboBox>
                            <ComboBox x:Name="HistoryUploadProviderCombo"
                                      MinWidth="138" Margin="0,0,8,0" VerticalAlignment="Center"
                                      ToolTip="Filter history by upload provider"
                                      AutomationProperties.Name="Upload provider filter"
                                      SelectionChanged="HistoryUploadProviderCombo_Changed"/>
                        </DockPanel>

                        <Grid x:Name="ImageSearchRow" Margin="0,8,0,0" Visibility="Visible">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                            <Border Background="{DynamicResource ThemeInputBackgroundBrush}"
                                    BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                                    BorderThickness="1" CornerRadius="7" Padding="10,6">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Image x:Name="ImageSearchIcon" Width="14" Height="14" Stretch="Uniform"
                                               VerticalAlignment="Center" Opacity="0.35"/>
                                        <Grid Grid.Column="1" Margin="8,0,0,0">
                                            <TextBox x:Name="ImageSearchBox" BorderThickness="0" Background="Transparent"
                                                     Padding="0" MinHeight="0" Height="20" VerticalContentAlignment="Center"
                                                     ToolTip="Search the current history category"
                                                     AutomationProperties.Name="History search"
                                                     TextChanged="ImageSearchBox_TextChanged"
                                                     PreviewKeyDown="ImageSearchBox_PreviewKeyDown"
                                                     GotKeyboardFocus="ImageSearchBox_FocusChanged"
                                                     LostKeyboardFocus="ImageSearchBox_FocusChanged"/>
                                            <TextBlock x:Name="ImageSearchPlaceholder" Text="Search screenshots, OCR text, or filenames..."
                                                       FontSize="12" Opacity="0.28" VerticalAlignment="Center"
                                                       IsHitTestVisible="False" Margin="0,0,0,0"/>
                                        </Grid>
                                    </Grid>

                                    <TextBlock x:Name="ImageSearchFiltersSummaryText" Visibility="Collapsed"/>
                                    <StackPanel x:Name="ReindexAllProgressPanel" Orientation="Horizontal" Visibility="Collapsed">
                                        <ProgressBar x:Name="ReindexAllProgressBar" Width="60" Height="3" IsIndeterminate="True" Visibility="Collapsed"/>
                                        <Button x:Name="ReindexAllBtn" Content="Index" Style="{StaticResource HistoryActionButton}"
                                                ToolTip="Refresh the image search index"
                                                AutomationProperties.Name="Refresh image search index"
                                                Cursor="Hand" Click="ReindexAllBtn_Click"/>
                                    </StackPanel>
                                    <ProgressBar x:Name="ImageSearchLoadingBar" Width="110" Height="3"
                                                 IsIndeterminate="True" Visibility="Collapsed"
                                                 Margin="22,6,0,0" HorizontalAlignment="Left"/>
                                </StackPanel>
                            </Border>
                            </StackPanel>
                            <Button x:Name="ImageSearchFiltersBtn" Grid.Column="1" Content="Filters" Style="{StaticResource HistoryActionButton}"
                                    Margin="8,0,0,0" VerticalAlignment="Stretch"
                                    ToolTip="Choose image search sources and exact matching"
                                    AutomationProperties.Name="Image search filters"
                                    AutomationProperties.HelpText="Open search source and exact-match options for History."
                                    Cursor="Hand" Click="ImageSearchFiltersBtn_Click">
                                <Button.ContextMenu>
                                    <ContextMenu x:Name="ImageSearchFiltersMenu" Placement="Bottom"
                                                 AutomationProperties.Name="Image search filter options">
                                        <MenuItem x:Name="ImageSearchFileNameCheck" Header="File name" IsCheckable="True"
                                                  ToolTip="Search screenshot file names"
                                                  AutomationProperties.Name="Search file names"
                                                  AutomationProperties.HelpText="Include screenshot file names in History search results."
                                                  Checked="ImageSearchSourcesCheck_Changed" Unchecked="ImageSearchSourcesCheck_Changed"/>
                                        <MenuItem x:Name="ImageSearchOcrCheck" Header="OCR text" IsCheckable="True"
                                                  ToolTip="Search recognized screenshot text"
                                                  AutomationProperties.Name="Search OCR text"
                                                  AutomationProperties.HelpText="Include recognized text from indexed screenshots in History search results."
                                                  Checked="ImageSearchSourcesCheck_Changed" Unchecked="ImageSearchSourcesCheck_Changed"/>
                                        <MenuItem x:Name="ImageSearchMetadataCheck" Header="App, window title, and tags" IsCheckable="True"
                                                  ToolTip="Search capture application, window title, and tags"
                                                  AutomationProperties.Name="Search capture metadata"
                                                  AutomationProperties.HelpText="Include application names, window titles, and tags in History search results."
                                                  Checked="ImageSearchSourcesCheck_Changed" Unchecked="ImageSearchSourcesCheck_Changed"/>
                                        <Separator/>
                                        <MenuItem x:Name="ImageSearchExactMatchCheck" Header="Exact match" IsCheckable="True"
                                                  ToolTip="Require exact phrase/token matches"
                                                  AutomationProperties.Name="Exact match search"
                                                  AutomationProperties.HelpText="Only show History search results that match the exact phrase or token."
                                                  Checked="ImageSearchExactMatchCheck_Changed" Unchecked="ImageSearchExactMatchCheck_Changed"/>
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>
                        </Grid>

                    </StackPanel>
                </Border>

                <!-- Images history -->
                <ScrollViewer x:Name="ImagesPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10,2,10,4"
                              ScrollChanged="HistoryPanel_ScrollChanged">
                    <StackPanel x:Name="HistoryStack"/>
                </ScrollViewer>

                <!-- Videos / GIF history -->
                <ScrollViewer x:Name="GifsPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10,2,10,4"
                              ScrollChanged="GifPanel_ScrollChanged"
                              Visibility="Collapsed">
                    <StackPanel x:Name="GifStack"/>
                </ScrollViewer>

                <!-- Text history -->
                <ScrollViewer x:Name="TextPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Padding="10,2,10,14" Visibility="Collapsed"
                              ScrollChanged="OcrPanel_ScrollChanged">
                    <StackPanel x:Name="OcrStack"/>
                </ScrollViewer>

                <!-- Color history -->
                <ScrollViewer x:Name="ColorsPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Padding="10,2,10,14" Visibility="Collapsed"
                              ScrollChanged="ColorsPanel_ScrollChanged">
                    <StackPanel x:Name="ColorStack"/>
                </ScrollViewer>

                <!-- Sticker history -->
                <ScrollViewer x:Name="StickersPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Padding="10,2,10,4" Visibility="Collapsed"
                              ScrollChanged="StickerPanel_ScrollChanged">
                    <StackPanel x:Name="StickerStack"/>
                </ScrollViewer>

                <!-- Code/QR history -->
                <ScrollViewer x:Name="CodesPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Padding="10,2,10,14" Visibility="Collapsed"
                              ScrollChanged="CodesPanel_ScrollChanged">
                    <StackPanel x:Name="CodeStack"/>
                </ScrollViewer>

                <!-- Empty states -->
                <StackPanel x:Name="HistoryEmptyText" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="Collapsed" Margin="0,0,0,40" MaxWidth="300">
                    <TextBlock x:Name="HistoryEmptyTitle" Text="No captures yet" FontSize="14"
                               FontFamily="Segoe UI Variable Text" FontWeight="SemiBold"
                               Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                               HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" Opacity="0.72"
                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                               AutomationProperties.Name="History empty state title"
                               AutomationProperties.HelpText="{Binding Text, RelativeSource={RelativeSource Self}}"
                               AutomationProperties.LiveSetting="Polite"/>
                    <TextBlock x:Name="HistoryEmptyLabel" Text="Screenshots will appear here after capture." FontSize="12.5"
                               FontFamily="Segoe UI Variable Text" Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                               Opacity="0.58" HorizontalAlignment="Center" TextAlignment="Center"
                               TextWrapping="Wrap" Margin="0,7,0,0"
                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                               AutomationProperties.Name="History empty state detail"
                               AutomationProperties.HelpText="{Binding Text, RelativeSource={RelativeSource Self}}"
                               AutomationProperties.LiveSetting="Polite"/>
                    <Button x:Name="HistoryEmptyRetryButton" Content="Retry" Style="{StaticResource HistoryActionButton}"
                            HorizontalAlignment="Center" Margin="0,14,0,0" Visibility="Collapsed"
                            ToolTip="Retry loading history"
                            AutomationProperties.Name="Retry loading history"
                            AutomationProperties.HelpText="Retry loading history"
                            Cursor="Hand" Click="HistoryEmptyRetryButton_Click"/>
                </StackPanel>

                <!-- Bottom bar -->
                <Border Grid.Row="2" Padding="14,4,14,8">
                    <DockPanel LastChildFill="False">
                        <TextBlock x:Name="HistoryCountText" FontSize="11" Opacity="0.4" VerticalAlignment="Center"
                                   DockPanel.Dock="Left"
                                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                   AutomationProperties.Name="History item count"
                                   AutomationProperties.HelpText="{Binding Text, RelativeSource={RelativeSource Self}}"
                                   AutomationProperties.LiveSetting="Polite"/>
                        <TextBlock x:Name="HistorySearchStatusText" FontSize="11" Opacity="0.35" VerticalAlignment="Center"
                                   HorizontalAlignment="Right" Margin="12,0,0,0"
                                   ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                   AutomationProperties.Name="History status"
                                   AutomationProperties.HelpText="{Binding Text, RelativeSource={RelativeSource Self}}"
                                   AutomationProperties.LiveSetting="Polite"/>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- Uploads -->
            <ScrollViewer x:Name="UploadsPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="26,58,26,22">
                <StackPanel>
                    <Border Padding="0,0,0,10">
                        <WrapPanel>
                            <RadioButton x:Name="UploadImagesSubTab" Content="Images" GroupName="UploadSub"
                                         IsChecked="True" Click="UploadSubTabChanged" Style="{StaticResource SubTab}"/>
                            <RadioButton x:Name="UploadAiRedirectsSubTab" Content="AI Redirects" GroupName="UploadSub"
                                         Click="UploadSubTabChanged" Margin="2,0,0,0" Style="{StaticResource SubTab}"/>
                            <RadioButton x:Name="UploadStickersSubTab" Content="Stickers" GroupName="UploadSub"
                                         Click="UploadSubTabChanged" Margin="2,0,0,0" Style="{StaticResource SubTab}"/>
                            <RadioButton x:Name="UploadUpscaleSubTab" Content="Upscale" GroupName="UploadSub"
                                         Click="UploadSubTabChanged" Margin="2,0,0,0" Style="{StaticResource SubTab}"/>
                        </WrapPanel>
                    </Border>

                    <StackPanel x:Name="ImageUploadsPanel">
                        <TextBlock Text="Destination" Style="{StaticResource SectionLabel}"/>
                        <Border Style="{StaticResource Card}">
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE898;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Upload service" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose where screenshots and captures are uploaded." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="UploadDestCombo" Grid.Column="2" MinWidth="145" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="280"
                                          AutomationProperties.Name="Upload service"
                                          ToolTip="Choose where screenshots and captures are uploaded."
                                          SelectionChanged="UploadDestCombo_Changed">
                                    <ComboBoxItem Content="None" Tag="0"/>
                                    <ComboBoxItem Content="Recommended: free/no-setup filter" Tag="21"/>
                                    <ComboBoxItem Content="Litterbox (temp)" Tag="4"/>
                                    <ComboBoxItem Content="tmpfiles.org (temp)" Tag="22"/>
                                    <ComboBoxItem Content="Uguu (temp)" Tag="7"/>
                                    <ComboBoxItem Content="Gofile (free)" Tag="23"/>
                                    <ComboBoxItem Content="Catbox (free)" Tag="3"/>
                                    <ComboBoxItem Content="file.io (temp)" Tag="6"/>
                                    <ComboBoxItem Content="Imgur (API/client ID)" Tag="1"/>
                                    <ComboBoxItem Content="ImgBB (API key)" Tag="2"/>
                                    <ComboBoxItem Content="imgpile (API token)" Tag="24"/>
                                    <ComboBoxItem Content="Gyazo (API token)" Tag="5"/>
                                    <ComboBoxItem Content="Dropbox" Tag="9"/>
                                    <ComboBoxItem Content="Google Drive" Tag="10"/>
                                    <ComboBoxItem Content="OneDrive" Tag="11"/>
                                    <ComboBoxItem Content="Azure Blob" Tag="12"/>
                                    <ComboBoxItem Content="GitHub" Tag="13"/>
                                    <ComboBoxItem Content="Immich" Tag="14"/>
                                    <ComboBoxItem Content="FTP" Tag="15"/>
                                    <ComboBoxItem Content="SFTP" Tag="16"/>
                                    <ComboBoxItem Content="WebDAV" Tag="17"/>
                                    <ComboBoxItem Content="S3 / R2 / B2" Tag="18"/>
                                    <ComboBoxItem Content="Custom HTTP" Tag="19"/>
                                </ComboBox>
                            </Grid>
                        </Border>

                    <!-- Imgur settings -->
                    <controls:ProviderSection x:Name="ImgurSettings" Header="IMGUR" IconSource="pack://application:,,,/Assets/imgur_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="PNG" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="JPG" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="GIF" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <TextBlock Text="  20MB images · 200MB GIF · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="Client ID" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                            <TextBox x:Name="ImgurClientIdBox"
                                     ToolTip="Optional — has built-in default for anonymous uploads"
                                     AutomationProperties.Name="Imgur client ID"
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     TextChanged="ImgurClientIdBox_Changed"/>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,10,0,10"/>

                            <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                            <PasswordBox x:Name="ImgurTokenBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch"
                                     ToolTip="Optional — for uploading to your Imgur account"
                                     AutomationProperties.Name="Imgur access token"
                                     AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     PasswordChanged="ImgurTokenBox_Changed"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <!-- ImgBB settings -->
                    <controls:ProviderSection x:Name="ImgBBSettings" Header="IMGBB" IconSource="pack://application:,,,/Assets/imgbb_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="PNG" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="JPG" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="GIF" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <TextBlock Text="  32MB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="API key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                            <PasswordBox x:Name="ImgBBKeyBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch"
                                     ToolTip="Get a free API key at api.imgbb.com"
                                     AutomationProperties.Name="ImgBB API key"
                                     AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     PasswordChanged="ImgBBKeyBox_Changed"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="ImgPileSettings" Header="IMGPILE" IconSource="pack://application:,,,/Assets/imgpile_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="Image" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="API token" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <TextBlock Text="  100MB max · Direct CDN URL" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="API token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                            <PasswordBox x:Name="ImgPileTokenBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch"
                                     AutomationProperties.Name="imgpile API token"
                                     AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     PasswordChanged="ImgPileTokenBox_Changed"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <!-- Catbox info -->
                    <controls:ProviderSection x:Name="CatboxSettings" Header="CATBOX" IconSource="pack://application:,,,/Assets/catbox_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <TextBlock Text="  200MB max · Free · Permanent" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="No setup required." Style="{StaticResource SettingsHelperText}"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <!-- Litterbox info -->
                    <controls:ProviderSection x:Name="LitterboxSettings" Header="LITTERBOX" IconSource="pack://application:,,,/Assets/litterbox_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChipDanger}">
                                    <TextBlock Text="Expires 72h" FontSize="9" Foreground="#EF4444"/>
                                </Border>
                                <TextBlock Text="  200MB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="Temporary host. No setup required." Style="{StaticResource SettingsHelperText}"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <!-- Gyazo settings -->
                    <controls:ProviderSection x:Name="GyazoSettings" Header="GYAZO" IconSource="pack://application:,,,/Assets/gyazo_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="PNG" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="JPG" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="GIF" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <TextBlock Text="  25MB max · Free tier + Pro $4/mo" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                            <PasswordBox x:Name="GyazoTokenBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch"
                                     AutomationProperties.Name="Gyazo access token"
                                     AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     PasswordChanged="GyazoTokenBox_Changed"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <!-- file.io info -->
                    <controls:ProviderSection x:Name="FileIoSettings" Header="FILE.IO" IconSource="pack://application:,,,/Assets/fileio_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChipDanger}">
                                    <TextBlock Text="1 download then deleted" FontSize="9" Foreground="#EF4444"/>
                                </Border>
                                <TextBlock Text="  100MB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="No setup required. Files delete after the first download." Style="{StaticResource SettingsHelperText}"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <!-- Uguu info -->
                    <controls:ProviderSection x:Name="UguuSettings" Header="UGUU" IconSource="pack://application:,,,/Assets/uguu_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChipDanger}">
                                    <TextBlock Text="Expires 48h" FontSize="9" Foreground="#EF4444"/>
                                </Border>
                                <TextBlock Text="  128MB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="Temporary host. No setup required." Style="{StaticResource SettingsHelperText}"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="TmpFilesSettings" Header="TMPFILES.ORG" IconSource="pack://application:,,,/Assets/tmpfiles_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChipDanger}">
                                    <TextBlock Text="Temporary" FontSize="9" Foreground="#EF4444"/>
                                </Border>
                                <TextBlock Text="  100MB max · No setup" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="No setup required. Used by the automatic free-host filter when it fits the file." Style="{StaticResource SettingsHelperText}"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="GofileSettings" Header="GOFILE" IconSource="pack://application:,,,/Assets/gofile_sq.png" Visibility="Collapsed">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <Border Style="{StaticResource InlineTagChip}">
                                    <TextBlock Text="No setup" FontSize="9" Opacity="0.6"/>
                                </Border>
                                <TextBlock Text="  Free public share page" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="No setup required. Used after direct temporary hosts in the automatic filter." Style="{StaticResource SettingsHelperText}"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <!-- S3-Compatible settings -->
                    <controls:ProviderSection x:Name="S3Settings" Header="S3 / R2 / B2" IconSource="pack://application:,,,/Assets/aws_sq.png" Visibility="Collapsed">
                        <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border Style="{StaticResource InlineTagChip}">
                                        <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <TextBlock Text="  AWS S3, Cloudflare R2, Backblaze B2, MinIO, and similar services" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>

                                <TextBlock Text="Endpoint URL" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="S3EndpointBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto"
                                         ToolTip="e.g. https://s3.us-east-1.amazonaws.com or https://your-id.r2.cloudflarestorage.com"
                                         AutomationProperties.Name="S3 endpoint URL"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                         TextChanged="S3EndpointBox_Changed"/>

                                <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,10,0,10"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Bucket" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox x:Name="S3BucketBox" MinWidth="145" MaxWidth="240" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto"
                                                 ToolTip="Bucket name for S3-compatible uploads"
                                                 AutomationProperties.Name="S3 bucket"
                                                 Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                                 TextChanged="S3BucketBox_Changed"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Region" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox x:Name="S3RegionBox" Text="auto" MinWidth="145" MaxWidth="240" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto"
                                                 ToolTip="Region for S3-compatible uploads, or auto"
                                                 AutomationProperties.Name="S3 region"
                                                 Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                                 TextChanged="S3RegionBox_Changed"/>
                                    </StackPanel>
                                </Grid>

                                <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,10,0,10"/>

                                <TextBlock Text="Access Key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="S3AccessKeyBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto"
                                         ToolTip="Access key ID for the selected S3-compatible service"
                                         AutomationProperties.Name="S3 access key"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                         TextChanged="S3AccessKeyBox_Changed"/>

                                <TextBlock Text="Secret Key" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <PasswordBox x:Name="S3SecretKeyBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch"
                                         AutomationProperties.Name="S3 secret key"
                                         AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                         PasswordChanged="S3SecretKeyBox_Changed"/>

                                <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,10,0,10"/>

                                <TextBlock Text="Public URL (optional)" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="S3PublicUrlBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto"
                                         ToolTip="Custom domain for public links, e.g. https://cdn.example.com"
                                         AutomationProperties.Name="S3 public URL"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                         TextChanged="S3PublicUrlBox_Changed"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="DropboxSettings" Header="DROPBOX" IconSource="pack://application:,,,/Assets/dropbox_sq.png" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <PasswordBox x:Name="DropboxTokenBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="DropboxTokenBox_Changed"
                                         AutomationProperties.Name="Dropbox access token"
                                         AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Folder / path prefix" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="DropboxPathBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="DropboxPathBox_Changed"
                                         ToolTip="Dropbox folder or path prefix for uploads"
                                         AutomationProperties.Name="Dropbox path prefix"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="GoogleDriveSettings" Header="GOOGLE DRIVE" IconSource="pack://application:,,,/Assets/gdrive_sq.png" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <PasswordBox x:Name="GoogleDriveTokenBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="GoogleDriveTokenBox_Changed"
                                         AutomationProperties.Name="Google Drive access token"
                                         AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Folder ID (optional)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="GoogleDriveFolderBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="GoogleDriveFolderBox_Changed"
                                         ToolTip="Optional Google Drive folder ID for uploads"
                                         AutomationProperties.Name="Google Drive folder ID"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="OneDriveSettings" Header="ONEDRIVE" IconSource="pack://application:,,,/Assets/onedrive_sq.png" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <PasswordBox x:Name="OneDriveTokenBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="OneDriveTokenBox_Changed"
                                         AutomationProperties.Name="OneDrive access token"
                                         AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Folder" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="OneDriveFolderBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="OneDriveFolderBox_Changed"
                                         ToolTip="OneDrive folder for uploads"
                                         AutomationProperties.Name="OneDrive folder"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="AzureSettings" Header="AZURE BLOB" IconSource="pack://application:,,,/Assets/azure_sq.png" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="Container SAS URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <PasswordBox x:Name="AzureBlobSasBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="AzureBlobSasBox_Changed"
                                         AutomationProperties.Name="Azure Blob SAS URL"
                                         AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="GitHubSettings" Header="GITHUB" IconSource="pack://application:,,,/Assets/github_sq.png" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="Token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <PasswordBox x:Name="GitHubTokenBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="GitHubTokenBox_Changed"
                                         AutomationProperties.Name="GitHub token"
                                         AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Repo (owner/name)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="GitHubRepoBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="GitHubRepoBox_Changed"
                                         ToolTip="GitHub repository in owner/name format"
                                         AutomationProperties.Name="GitHub repository"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Branch" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="GitHubBranchBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="GitHubBranchBox_Changed"
                                         ToolTip="GitHub branch used for uploads"
                                         AutomationProperties.Name="GitHub branch"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="ImmichSettings" Header="IMMICH" IconSource="pack://application:,,,/Assets/immich_sq.png" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="Base URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="ImmichUrlBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="ImmichUrlBox_Changed"
                                         ToolTip="Immich server base URL"
                                         AutomationProperties.Name="Immich base URL"
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="API key" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <PasswordBox x:Name="ImmichApiKeyBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="ImmichApiKeyBox_Changed"
                                         AutomationProperties.Name="Immich API key"
                                         AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it."
                                         Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="FtpSettings" Header="FTP" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="FTP URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="FtpUrlBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="FtpUrlBox_Changed" ToolTip="FTP upload URL" AutomationProperties.Name="FTP URL" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Username" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="FtpUsernameBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="FtpUsernameBox_Changed" ToolTip="Username for FTP uploads" AutomationProperties.Name="FTP username" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Password" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <PasswordBox x:Name="FtpPasswordBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="FtpPasswordBox_Changed" AutomationProperties.Name="FTP password" AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it." Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Public URL base (optional)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="FtpPublicUrlBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="FtpPublicUrlBox_Changed" ToolTip="Public base URL for FTP uploads" AutomationProperties.Name="FTP public URL base" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="SftpSettings" Header="SFTP" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="Host" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="SftpHostBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="SftpHostBox_Changed" ToolTip="SFTP host name or IP address" AutomationProperties.Name="SFTP host" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Port" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpPortBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="SftpPortBox_Changed" ToolTip="SFTP port number" AutomationProperties.Name="SFTP port" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Username" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpUsernameBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="SftpUsernameBox_Changed" ToolTip="Username for SFTP uploads" AutomationProperties.Name="SFTP username" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Password" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <PasswordBox x:Name="SftpPasswordBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="SftpPasswordBox_Changed" AutomationProperties.Name="SFTP password" AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it." Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Remote path" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpRemotePathBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="SftpRemotePathBox_Changed" ToolTip="Remote folder path for SFTP uploads" AutomationProperties.Name="SFTP remote path" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Public URL base (optional)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpPublicUrlBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="SftpPublicUrlBox_Changed" ToolTip="Public base URL for SFTP uploads" AutomationProperties.Name="SFTP public URL base" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Server fingerprint (SHA256 hex)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpHostKeyFingerprintBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="SftpHostKeyFingerprintBox_Changed" ToolTip="Expected SFTP server SHA256 fingerprint" AutomationProperties.Name="SFTP host key fingerprint" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <controls:ProviderSection x:Name="WebDavSettings" Header="WEBDAV" Visibility="Collapsed">
                        <StackPanel>
                                <TextBlock Text="WebDAV URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="WebDavUrlBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="WebDavUrlBox_Changed" ToolTip="WebDAV upload URL" AutomationProperties.Name="WebDAV URL" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Username" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="WebDavUsernameBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="WebDavUsernameBox_Changed" ToolTip="Username for WebDAV uploads" AutomationProperties.Name="WebDAV username" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Password" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <PasswordBox x:Name="WebDavPasswordBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" PasswordChanged="WebDavPasswordBox_Changed" AutomationProperties.Name="WebDAV password" AutomationProperties.HelpText="Hidden for safety. Paste a new value to replace it." Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                                <TextBlock Text="Public URL base (optional)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="WebDavPublicUrlBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" TextChanged="WebDavPublicUrlBox_Changed" ToolTip="Public base URL for WebDAV uploads" AutomationProperties.Name="WebDAV public URL base" Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <!-- Custom HTTP settings -->
                    <controls:ProviderSection x:Name="CustomUploadSettings" Header="CUSTOM HTTP" Visibility="Collapsed">
                        <controls:ProviderSection.HeaderExtras>
                            <Border Width="16" Height="16" CornerRadius="3" Background="#5865F2" Margin="0,0,6,0" VerticalAlignment="Center">
                                <TextBlock Text="&#x2B06;" FontSize="9" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                            </Border>
                        </controls:ProviderSection.HeaderExtras>
                        <StackPanel>
                            <TextBlock Text="Upload URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                            <TextBox x:Name="CustomUrlBox" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto"
                                     ToolTip="Custom HTTP endpoint for uploads"
                                     AutomationProperties.Name="Custom upload URL"
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     TextChanged="CustomUrlBox_Changed"/>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,10,0,10"/>

                            <TextBlock Text="File form field name" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                            <TextBox x:Name="CustomFieldBox" Text="file" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto"
                                     ToolTip="Multipart form field name for the uploaded file"
                                     AutomationProperties.Name="Custom file form field"
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     TextChanged="CustomFieldBox_Changed"/>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,10,0,10"/>

                            <TextBlock Text="Response JSON path to URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                            <TextBox x:Name="CustomJsonPathBox" Text="url" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto"
                                     ToolTip="Dot-separated path, e.g. data.url or just url"
                                     AutomationProperties.Name="Custom response URL JSON path"
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     TextChanged="CustomJsonPathBox_Changed"/>

                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,10,0,10"/>

                            <TextBlock Text="Custom headers" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                            <TextBox x:Name="CustomHeadersBox" AcceptsReturn="True" MinWidth="145" MaxWidth="360" HorizontalAlignment="Stretch"
                                     ToolTip="One per line, format: Name: Value"
                                     AutomationProperties.Name="Custom upload headers"
                                     Height="70" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                     Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                     TextChanged="CustomHeadersBox_Changed"/>
                        </StackPanel>
                    </controls:ProviderSection>

                    <TextBlock x:Name="AutoUploadHeader" Text="Auto-upload" Style="{StaticResource SectionLabel}" Margin="4,18,0,9"/>

                    <Border x:Name="AutoUploadCard" Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE722;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Auto-upload screenshots" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Upload images immediately after capture." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="AutoUploadScreenshotsCheck" Grid.Column="2" Content="" FontSize="13"
                                          ToolTip="Uploads screenshots after capture"
                                          AutomationProperties.Name="Auto-upload screenshots"
                                          Checked="AutoUploadScreenshotsCheck_Changed" Unchecked="AutoUploadScreenshotsCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE714;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Auto-upload GIFs" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Upload animated GIF captures after recording." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="AutoUploadGifsCheck" Grid.Column="2" Content="" FontSize="13"
                                          ToolTip="Uploads GIF captures after recording"
                                          AutomationProperties.Name="Auto-upload GIFs"
                                          Checked="AutoUploadGifsCheck_Changed" Unchecked="AutoUploadGifsCheck_Changed"/>
                            </Grid>
                            <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE714;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Auto-upload videos" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Upload video captures when recording completes." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <CheckBox x:Name="AutoUploadVideosCheck" Grid.Column="2" Content="" FontSize="13"
                                          ToolTip="Uploads video captures when recording completes"
                                          AutomationProperties.Name="Auto-upload videos"
                                          Checked="AutoUploadVideosCheck_Changed" Unchecked="AutoUploadVideosCheck_Changed"/>
                            </Grid>
                            <TextBlock x:Name="AutoUploadStatusText"
                                       Style="{StaticResource SettingsStatusText}"
                                       Visibility="Collapsed"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                       AutomationProperties.Name="Auto-upload status"
                                       AutomationProperties.LiveSetting="Polite"
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Test upload -->
                    <TextBlock Text="Test" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"
                               Visibility="{Binding Visibility, ElementName=TestUploadCard}"/>
                    <Border Style="{StaticResource Card}" x:Name="TestUploadCard" Visibility="Collapsed">
                        <Grid Style="{StaticResource SettingRow}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Style="{StaticResource SettingIconFrame}">
                                <TextBlock Text="&#xE9D5;" Style="{StaticResource SettingIconGlyph}"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Test upload" Style="{StaticResource SettingTitle}"/>
                                <TextBlock Text="Send a tiny image to verify this upload setup." Style="{StaticResource SettingDescription}"/>
                            </StackPanel>
                            <Button x:Name="TestUploadBtn" Grid.Column="2" Content="Test Upload"
                                    ToolTip="Sends a tiny 1x1 pixel image to test your configuration"
                                    AutomationProperties.Name="Test upload"
                                    Click="TestUpload_Click" Cursor="Hand"/>
                            <TextBlock x:Name="TestUploadStatusText" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                       Style="{StaticResource SettingsStatusText}" Visibility="Collapsed"
                                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                       AutomationProperties.Name="Test upload status"
                                       AutomationProperties.LiveSetting="Polite"
                                       Margin="0,8,0,0"/>
                        </Grid>
                    </Border>
                    </StackPanel>

                    <StackPanel x:Name="AiRedirectUploadsPanel" Visibility="Collapsed">
                        <TextBlock Text="Destination" Style="{StaticResource SectionLabel}"/>
                        <Border Style="{StaticResource Card}">
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE950;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="AI tool" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Choose the app OddSnap opens after an AI redirect capture." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <ComboBox x:Name="AiRedirectProviderCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="240"
                                          AutomationProperties.Name="AI tool"
                                          ToolTip="Choose the app OddSnap opens after an AI redirect capture."
                                          SelectionChanged="AiRedirectProviderCombo_Changed">
                                    <ComboBoxItem Content="None" Tag="-1"/>
                                    <ComboBoxItem Content="ChatGPT" Tag="0"/>
                                    <ComboBoxItem Content="Claude" Tag="1"/>
                                    <ComboBoxItem Content="Gemini" Tag="3"/>
                                    <ComboBoxItem Content="Google Lens" Tag="4"/>
                                </ComboBox>
                            </Grid>
                        </Border>

                        <TextBlock Text="Hotkey" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                        <Border Style="{StaticResource Card}">
                            <Grid x:Name="AiRedirectHotkeyPanelRow" Style="{StaticResource SettingRow}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE765;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="AI Redirect hotkey" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Capture and send a region to the selected AI tool." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBox x:Name="AiRedirectPanelHotkeyBox"
                                             Style="{StaticResource HotkeyBox}"
                                             ToolTip="Press a key combination for AI Redirect"
                                             AutomationProperties.Name="AI Redirect hotkey"
                                             GotFocus="AiRedirectPanelHotkeyBox_GotFocus"
                                             LostFocus="AiRedirectPanelHotkeyBox_LostFocus"
                                             PreviewKeyDown="AiRedirectPanelHotkeyBox_PreviewKeyDown"
                                             PreviewKeyUp="AiRedirectPanelHotkeyBox_PreviewKeyUp"/>
                                    <Button x:Name="AiRedirectPanelHotkeyClearBtn"
                                            Content="×"
                                            Style="{StaticResource ClearBtn}"
                                            ToolTip="Clear AI Redirect hotkey"
                                            AutomationProperties.Name="Clear AI Redirect hotkey"
                                            Cursor="Hand"
                                            Click="AiRedirectPanelHotkeyClearBtn_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <TextBlock Text="Test" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"
                                   Visibility="{Binding Visibility, ElementName=AiRedirectTestCard}"/>
                        <Border Style="{StaticResource Card}" x:Name="AiRedirectTestCard" Visibility="Collapsed">
                            <Grid Style="{StaticResource SettingRow}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Style="{StaticResource SettingIconFrame}">
                                    <TextBlock Text="&#xE8A7;" Style="{StaticResource SettingIconGlyph}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Test AI redirect" Style="{StaticResource SettingTitle}"/>
                                    <TextBlock Text="Verify the selected AI tool opens with the current redirect setup." Style="{StaticResource SettingDescription}"/>
                                </StackPanel>
                                <Button x:Name="AiRedirectTestBtn" Grid.Column="2" Content="Test Redirect"
                                        ToolTip="Opens the selected AI tool; Google Lens also tests the hosted image upload"
                                        AutomationProperties.Name="Test AI redirect"
                                        Click="AiRedirectTestBtn_Click" Cursor="Hand"/>
                                <TextBlock x:Name="AiRedirectTestStatusText" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                                           Style="{StaticResource SettingsStatusText}" Visibility="Collapsed"
                                           ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                           AutomationProperties.Name="AI redirect test status"
                                           AutomationProperties.LiveSetting="Polite"
                                           Margin="0,8,0,0"/>
                            </Grid>
                        </Border>

                        <StackPanel x:Name="AiRedirectLensUploadHostPanelRow" Visibility="Collapsed">
                            <TextBlock Text="Hosted Image" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>
                            <Border Style="{StaticResource Card}">
                                <StackPanel>
                                    <Grid Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE8AB;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Synced with Images uploads" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Use the same image host for Google Lens redirects." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <CheckBox x:Name="AiRedirectLensUploadSyncCheck" Grid.Column="2"
                                                  Content=""
                                                  FontSize="13"
                                                  AutomationProperties.Name="Synced with Images uploads"
                                                  ToolTip="Use the same image host for Google Lens redirects."
                                                  Checked="AiRedirectLensUploadSyncCheck_Changed"
                                                  Unchecked="AiRedirectLensUploadSyncCheck_Changed"/>
                                    </Grid>
                                    <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                                    <Grid Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE898;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Lens upload service" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Pick the public host used before opening Google Lens." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <ComboBox x:Name="AiRedirectLensUploadDestPanelCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="280"
                                                  AutomationProperties.Name="Lens upload service"
                                                  ToolTip="Pick the public host used before opening Google Lens."
                                                  SelectionChanged="AiRedirectLensUploadDestPanelCombo_Changed"/>
                                    </Grid>
                                    <TextBlock x:Name="AiRedirectLensUploadPanelHint" Visibility="Collapsed"
                                               Text="Google Lens needs a public image URL, so OddSnap uploads first and then opens Lens."
                                               Style="{StaticResource SettingsHelperText}" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel x:Name="StickerUploadsPanel" Visibility="Collapsed">
                        <TextBlock Text="Provider" Style="{StaticResource SectionLabel}"/>
                        <Border Style="{StaticResource Card}">
                                <Grid Style="{StaticResource SettingRow}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Style="{StaticResource SettingIconFrame}">
                                        <TextBlock Text="&#xE91B;" Style="{StaticResource SettingIconGlyph}"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Sticker provider" Style="{StaticResource SettingTitle}"/>
                                        <TextBlock Text="Choose the background removal engine for sticker captures." Style="{StaticResource SettingDescription}"/>
                                    </StackPanel>
                                    <ComboBox x:Name="StickerProviderCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="240"
                                              AutomationProperties.Name="Sticker provider"
                                              ToolTip="Choose the background removal engine for sticker captures."
                                              SelectionChanged="StickerProviderCombo_Changed">
                                        <ComboBoxItem Content="None"/>
                                        <ComboBoxItem Content="Remove.bg"/>
                                        <ComboBoxItem Content="Photoroom"/>
                                        <ComboBoxItem Content="Local (CPU/GPU)"/>
                                    </ComboBox>
                                </Grid>
                        </Border>

                                <StackPanel x:Name="StickerRemoveBgPanel" Visibility="Collapsed">
                                    <TextBlock Text="remove.bg API key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                    <PasswordBox x:Name="StickerRemoveBgKeyBox"
                                             AutomationProperties.Name="Remove.bg API key"
                                             Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                             PasswordChanged="StickerRemoveBgKeyBox_Changed"/>
                                </StackPanel>

                                <StackPanel x:Name="StickerPhotoroomPanel" Visibility="Collapsed">
                                    <TextBlock Text="Photoroom API key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                    <PasswordBox x:Name="StickerPhotoroomKeyBox"
                                             AutomationProperties.Name="Photoroom API key"
                                             Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                             PasswordChanged="StickerPhotoroomKeyBox_Changed"/>
                                </StackPanel>

                                <StackPanel x:Name="StickerLocalCpuPanel" Visibility="Collapsed">
                                    <TextBlock Text="Local Models" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>

                                    <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                    <Grid Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE950;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Execution mode" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Choose CPU or CUDA acceleration for local stickers." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <ComboBox x:Name="StickerLocalExecutionCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="220"
                                                  AutomationProperties.Name="Sticker execution mode"
                                                  ToolTip="Choose CPU or CUDA acceleration for local stickers."
                                                  SelectionChanged="StickerLocalExecutionCombo_Changed">
                                            <ComboBoxItem Content="CPU"/>
                                            <ComboBoxItem Content="GPU (CUDA)"/>
                                        </ComboBox>
                                    </Grid>

                                    <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>

                                    <Grid x:Name="StickerLocalCpuEnginePanel" Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE964;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="CPU model" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Pick the local model used without GPU acceleration." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <ComboBox x:Name="StickerLocalCpuEngineCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="240"
                                                      AutomationProperties.Name="Sticker CPU model"
                                                      ToolTip="Pick the local model used without GPU acceleration."
                                                      SelectionChanged="StickerLocalCpuEngineCombo_Changed">
                                            <ComboBoxItem Content="BRIA RMBG" Tag="BriaRmbg"/>
                                            <ComboBoxItem Content="BiRefNet Lite (High quality)" Tag="BiRefNetLite"/>
                                            <ComboBoxItem Content="ISNet General Use (Balanced)" Tag="IsNetGeneralUse"/>
                                            <ComboBoxItem Content="U2Netp (Fastest)" Tag="U2Netp"/>
                                        </ComboBox>
                                    </Grid>

                                    <Grid x:Name="StickerLocalGpuEnginePanel" Style="{StaticResource SettingRow}" Visibility="Collapsed">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE950;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="GPU model" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Pick the CUDA model used for local stickers." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <ComboBox x:Name="StickerLocalGpuEngineCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="240"
                                                      AutomationProperties.Name="Sticker GPU model"
                                                      ToolTip="Pick the CUDA model used for local stickers."
                                                      SelectionChanged="StickerLocalGpuEngineCombo_Changed">
                                            <ComboBoxItem Content="BRIA RMBG" Tag="BriaRmbg"/>
                                            <ComboBoxItem Content="BiRefNet Lite (High quality)" Tag="BiRefNetLite"/>
                                            <ComboBoxItem Content="ISNet General Use (Balanced)" Tag="IsNetGeneralUse"/>
                                            <ComboBoxItem Content="U2Netp (Fastest)" Tag="U2Netp"/>
                                        </ComboBox>
                                    </Grid>

                                    <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,10,0,10"/>

                                    <TextBlock x:Name="StickerLocalEngineStatusText" Text="Pick a model. Models download automatically on first use." Style="{StaticResource SettingsStatusText}"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Sticker local runtime status"
                                               AutomationProperties.LiveSetting="Polite"/>
                                    <ProgressBar x:Name="StickerLocalEngineProgress" Height="6" Margin="0,8,0,0" Minimum="0" Maximum="100" Visibility="Collapsed"/>
                                    <TextBlock x:Name="StickerLocalEngineProgressText" Style="{StaticResource SettingsProgressText}" Visibility="Collapsed"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Sticker local runtime progress"
                                               AutomationProperties.LiveSetting="Polite"/>
                                    <WrapPanel Margin="0,10,0,-8">
                                        <Button x:Name="StickerInstallDriversBtn" Content="Install runtime" Margin="0,0,8,8"
                                                ToolTip="Install or remove the local sticker runtime"
                                                AutomationProperties.Name="Install sticker runtime"
                                                Click="StickerInstallDriversBtn_Click" Cursor="Hand"/>
                                        <Button x:Name="StickerDownloadRembgBtn" Content="Download model"
                                                Margin="0,0,8,8" ToolTip="Download the selected local sticker model"
                                                AutomationProperties.Name="Download sticker model"
                                                Click="StickerDownloadRembgBtn_Click" Cursor="Hand"/>
                                        <Button x:Name="StickerOpenLocalEngineRepoBtn" Content="Open engine project" Margin="0,0,8,8"
                                                ToolTip="Open the selected sticker engine project"
                                                AutomationProperties.Name="Open sticker engine project"
                                                Click="StickerOpenLocalEngineRepoBtn_Click" Cursor="Hand"/>
                                        <Button x:Name="StickerRemoveAllModelsBtn" Content="Remove all models" Margin="0,0,8,8"
                                                ToolTip="Remove all downloaded sticker models"
                                                AutomationProperties.Name="Remove sticker models"
                                                Click="StickerRemoveAllModelsBtn_Click" Cursor="Hand" Visibility="Collapsed"/>
                                        <Button x:Name="StickerCopyErrorBtn" Content="Copy details" Margin="0,0,8,8"
                                                ToolTip="Copy sticker setup error details"
                                                AutomationProperties.Name="Copy sticker setup details"
                                                Click="StickerCopyErrorBtn_Click" Cursor="Hand" Visibility="Collapsed"/>
                                    </WrapPanel>
                                    </StackPanel>
                                    </Border>

                                    <TextBlock Text="Output" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>

                                    <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                    <Grid Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE790;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Add drop shadow" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Give generated stickers a subtle shadow." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <CheckBox x:Name="StickerShadowCheck" Grid.Column="2" Content="" FontSize="13"
                                                  AutomationProperties.Name="Add drop shadow"
                                                  ToolTip="Give generated stickers a subtle shadow."
                                                  Checked="StickerShadowCheck_Changed" Unchecked="StickerShadowCheck_Changed"/>
                                    </Grid>
                                    <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                                    <Grid Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE9A6;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Add white stroke" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Add a clean border around generated stickers." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <CheckBox x:Name="StickerStrokeCheck" Grid.Column="2" Content="" FontSize="13"
                                                  AutomationProperties.Name="Add white stroke"
                                                  ToolTip="Add a clean border around generated stickers."
                                                  Checked="StickerStrokeCheck_Changed" Unchecked="StickerStrokeCheck_Changed"/>
                                    </Grid>
                                    </StackPanel>
                                    </Border>
                                </StackPanel>
                    </StackPanel>

                    <StackPanel x:Name="UpscaleUploadsPanel" Visibility="Collapsed">
                        <TextBlock Text="Provider" Style="{StaticResource SectionLabel}"/>
                        <Border Style="{StaticResource Card}">
                                <Grid Style="{StaticResource SettingRow}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Style="{StaticResource SettingIconFrame}">
                                        <TextBlock Text="&#xE7B3;" Style="{StaticResource SettingIconGlyph}"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Upscale provider" Style="{StaticResource SettingTitle}"/>
                                        <TextBlock Text="Choose the engine used for upscale captures." Style="{StaticResource SettingDescription}"/>
                                    </StackPanel>
                                    <ComboBox x:Name="UpscaleProviderCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="240"
                                              AutomationProperties.Name="Upscale provider"
                                              ToolTip="Choose the engine used for upscale captures."
                                              SelectionChanged="UpscaleProviderCombo_Changed">
                                        <ComboBoxItem Content="None"/>
                                        <ComboBoxItem Content="Local (CPU/GPU)"/>
                                        <ComboBoxItem Content="DeepAI Super Resolution"/>
                                        <ComboBoxItem Content="DeepAI Waifu2x"/>
                                    </ComboBox>
                                </Grid>
                        </Border>

                                <StackPanel x:Name="UpscaleDeepAiPanel" Visibility="Collapsed" Margin="0,0,0,10">
                                    <TextBlock Text="DeepAI API key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                    <PasswordBox x:Name="UpscaleDeepAiApiKeyBox"
                                             AutomationProperties.Name="DeepAI API key"
                                             Background="{DynamicResource ThemeInputBackgroundBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeInputBorderBrush}" BorderThickness="1"
                                             PasswordChanged="UpscaleDeepAiApiKeyBox_Changed"/>
                                    <TextBlock Text="Optional cloud providers. Local remains the default." Style="{StaticResource SettingsHelperText}" Margin="0,8,0,0"/>
                                </StackPanel>

                                <StackPanel x:Name="UpscaleLocalPanel" Visibility="Collapsed">
                                    <TextBlock Text="Local Models" Style="{StaticResource SectionLabel}" Margin="4,16,0,9"/>

                                    <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                    <Grid Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE950;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Execution mode" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Choose CPU or CUDA acceleration for local upscaling." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <ComboBox x:Name="UpscaleLocalExecutionCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="220"
                                                  AutomationProperties.Name="Upscale execution mode"
                                                  ToolTip="Choose CPU or CUDA acceleration for local upscaling."
                                                  SelectionChanged="UpscaleLocalExecutionCombo_Changed">
                                            <ComboBoxItem Content="CPU"/>
                                            <ComboBoxItem Content="GPU (CUDA)"/>
                                        </ComboBox>
                                    </Grid>

                                    <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>

                                    <Grid x:Name="UpscaleLocalCpuEnginePanel" Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE964;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="CPU model" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Pick the local model used without GPU acceleration." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <ComboBox x:Name="UpscaleLocalCpuEngineCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="220"
                                                      AutomationProperties.Name="Upscale CPU model"
                                                      ToolTip="Pick the local model used without GPU acceleration."
                                                      SelectionChanged="UpscaleLocalCpuEngineCombo_Changed">
                                            <ComboBoxItem Content="SwinIR x4 (Faithful / UI)" Tag="SwinIrRealWorld"/>
                                            <ComboBoxItem Content="Real-ESRGAN x4plus (Heavier)" Tag="RealEsrganX4Plus"/>
                                        </ComboBox>
                                    </Grid>

                                    <Grid x:Name="UpscaleLocalGpuEnginePanel" Style="{StaticResource SettingRow}" Visibility="Collapsed">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE950;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="GPU model" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Pick the CUDA model used for local upscaling." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <ComboBox x:Name="UpscaleLocalGpuEngineCombo" Grid.Column="2" MinWidth="140" MaxWidth="240" HorizontalAlignment="Stretch" MaxDropDownHeight="220"
                                                      AutomationProperties.Name="Upscale GPU model"
                                                      ToolTip="Pick the CUDA model used for local upscaling."
                                                      SelectionChanged="UpscaleLocalGpuEngineCombo_Changed">
                                            <ComboBoxItem Content="Real-ESRGAN x4plus (Best quality)" Tag="RealEsrganX4Plus"/>
                                            <ComboBoxItem Content="SwinIR x4 (Faithful / UI)" Tag="SwinIrRealWorld"/>
                                        </ComboBox>
                                    </Grid>
                                    <TextBlock x:Name="UpscaleLocalEngineStatusText" Text="Pick a model. Models download automatically on first use." Style="{StaticResource SettingsStatusText}"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Upscale local runtime status"
                                               AutomationProperties.LiveSetting="Polite"/>
                                    <ProgressBar x:Name="UpscaleLocalEngineProgress" Height="6" Margin="0,8,0,0" Minimum="0" Maximum="100" Visibility="Collapsed"/>
                                    <TextBlock x:Name="UpscaleLocalEngineProgressText" Style="{StaticResource SettingsProgressText}" Visibility="Collapsed"
                                               ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"
                                               AutomationProperties.Name="Upscale local runtime progress"
                                               AutomationProperties.LiveSetting="Polite"/>
                                    <WrapPanel Margin="0,10,0,-8">
                                        <Button x:Name="UpscaleInstallDriversBtn" Content="Install runtime" Margin="0,0,8,8"
                                                ToolTip="Install or remove the local upscale runtime"
                                                AutomationProperties.Name="Install upscale runtime"
                                                Click="UpscaleInstallDriversBtn_Click" Cursor="Hand"/>
                                        <Button x:Name="UpscaleDownloadModelBtn" Content="Download model"
                                                Margin="0,0,8,8" ToolTip="Download the selected local upscale model"
                                                AutomationProperties.Name="Download upscale model"
                                                Click="UpscaleDownloadModelBtn_Click" Cursor="Hand"/>
                                        <Button x:Name="UpscaleOpenLocalEngineRepoBtn" Content="Open engine project" Margin="0,0,8,8"
                                                ToolTip="Open the selected upscale engine project"
                                                AutomationProperties.Name="Open upscale engine project"
                                                Click="UpscaleOpenLocalEngineRepoBtn_Click" Cursor="Hand"/>
                                        <Button x:Name="UpscaleRemoveAllModelsBtn" Content="Remove all models" Margin="0,0,8,8"
                                                ToolTip="Remove all downloaded upscale models"
                                                AutomationProperties.Name="Remove upscale models"
                                                Click="UpscaleRemoveAllModelsBtn_Click" Cursor="Hand" Visibility="Collapsed"/>
                                        <Button x:Name="UpscaleCopyErrorBtn" Content="Copy details" Margin="0,0,8,8"
                                                ToolTip="Copy upscale setup error details"
                                                AutomationProperties.Name="Copy upscale setup details"
                                                Click="UpscaleCopyErrorBtn_Click" Cursor="Hand" Visibility="Collapsed"/>
                                    </WrapPanel>
                                    </StackPanel>
                                    </Border>

                                    <TextBlock Text="Result Behavior" Style="{StaticResource SectionLabel}" Margin="4,8,0,9"/>

                                    <Border Style="{StaticResource Card}">
                                    <StackPanel>
                                    <Grid Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE8A7;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Show preview window" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock Text="Open the upscale preview after selecting an image." Style="{StaticResource SettingDescription}"/>
                                        </StackPanel>
                                        <CheckBox x:Name="UpscaleShowPreviewWindowCheck"
                                                  Grid.Column="2"
                                                  Content=""
                                                  FontSize="13"
                                                  AutomationProperties.Name="Show preview window"
                                                  ToolTip="Open the upscale preview after selecting an image."
                                                  Checked="UpscaleShowPreviewWindowCheck_Changed"
                                                  Unchecked="UpscaleShowPreviewWindowCheck_Changed"/>
                                    </Grid>

                                    <Border Height="1" Background="{DynamicResource ThemeSeparatorBrush}" Margin="0,9,0,9"/>
                                    <Grid Style="{StaticResource SettingRow}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Style="{StaticResource SettingIconFrame}">
                                            <TextBlock Text="&#xE8A7;" Style="{StaticResource SettingIconGlyph}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="Default multiplier" Style="{StaticResource SettingTitle}"/>
                                            <TextBlock x:Name="UpscaleDefaultScaleHelpText"
                                                       Text="Used when preview is off."
                                                       Style="{StaticResource SettingDescription}"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <ComboBox x:Name="UpscaleDefaultScaleCombo"
                                                  Grid.Column="2"
                                                 
                                                 
                                                  MinWidth="120"
                                                  Margin="10,0,0,0"
                                                  AutomationProperties.Name="Default multiplier"
                                                  ToolTip="Used when preview is off."
                                                  SelectionChanged="UpscaleDefaultScaleCombo_Changed"/>
                                    </Grid>
                                    </StackPanel>
                                    </Border>

                                </StackPanel>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
    </Grid>
    </Border>
    </Border>
</Window>
