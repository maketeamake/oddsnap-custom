<Window x:Class="OddSnap.UI.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Background="Transparent"
        SizeToContent="WidthAndHeight"
        Opacity="0"
        MouseEnter="OnMouseEnter"
        MouseLeave="OnMouseLeave">

    <Border x:Name="ImageBorder" MinWidth="120" MinHeight="80"
            CornerRadius="18" Background="#12000000"
            BorderBrush="#D8FFFFFF" BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect BlurRadius="32" ShadowDepth="6" Opacity="0.4" Color="Black"/>
        </Border.Effect>
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform x:Name="DragScale" ScaleX="1" ScaleY="1"/>
                <TranslateTransform x:Name="SlideX" X="0"/>
            </TransformGroup>
        </Border.RenderTransform>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="PreviewFrame" Grid.Row="0"
                    Margin="4,4,4,0"
                    CornerRadius="14" Background="#1B1B1A" ClipToBounds="True">
                <Border.Clip>
                    <RectangleGeometry x:Name="ImageClip" RadiusX="14" RadiusY="14"/>
                </Border.Clip>
                <Grid>
                    <Image x:Name="ThumbnailImage" Stretch="Uniform"
                           ToolTip="Screenshot preview"
                           AutomationProperties.Name="Screenshot preview"
                           AutomationProperties.HelpText="Screenshot preview."
                           RenderOptions.BitmapScalingMode="HighQuality"/>

                    <Border x:Name="CloseBtn" Width="40" Height="40" CornerRadius="12"
                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Cursor="Hand"
                            Focusable="True"
                            MouseLeftButtonDown="CloseClick"
                            KeyDown="CloseBtn_KeyDown"
                            HorizontalAlignment="Right" VerticalAlignment="Top"
                            Margin="0,8,8,0" Opacity="0"
                            ToolTip="Close preview"
                            AutomationProperties.Name="Close preview"
                            AutomationProperties.HelpText="Close this preview.">
                        <Image x:Name="CloseIcon" Width="18" Height="18" Stretch="Uniform" Opacity="1"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <Border x:Name="PinBtn" Width="40" Height="40" CornerRadius="12"
                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Cursor="Hand"
                            Focusable="True"
                            MouseLeftButtonDown="PinClick"
                            KeyDown="PinBtn_KeyDown"
                            HorizontalAlignment="Left" VerticalAlignment="Bottom"
                            Margin="8,0,0,8" Opacity="0"
                            ToolTip="Pin preview"
                            AutomationProperties.Name="Pin preview"
                            AutomationProperties.HelpText="Keep this preview open.">
                        <Image x:Name="PinIcon" Width="18" Height="18" Stretch="Uniform" Opacity="1"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <Border x:Name="SaveBtn" Width="40" Height="40" CornerRadius="12"
                            Background="Transparent" BorderBrush="Transparent" BorderThickness="0" Cursor="Hand"
                            Focusable="True"
                            MouseLeftButtonDown="SaveClick"
                            KeyDown="SaveBtn_KeyDown"
                            HorizontalAlignment="Right" VerticalAlignment="Bottom"
                            Margin="0,0,8,8" Opacity="0"
                            ToolTip="Save preview"
                            AutomationProperties.Name="Save preview"
                            AutomationProperties.HelpText="Save this preview image.">
                        <Image x:Name="SaveIcon" Width="18" Height="18" Stretch="Uniform" Opacity="1"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border x:Name="ProgressBar" Grid.Row="1" Height="2"
                    Margin="4,0,4,4"
                    Background="#55FFFFFF" CornerRadius="0,0,14,14"
                    HorizontalAlignment="Stretch"
                    RenderTransformOrigin="0,0.5">
                <Border.RenderTransform>
                    <ScaleTransform x:Name="ProgressScale" ScaleX="1" ScaleY="1"/>
                </Border.RenderTransform>
            </Border>
        </Grid>
    </Border>
</Window>
