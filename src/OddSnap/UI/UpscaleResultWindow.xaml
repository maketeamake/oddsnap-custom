<Window x:Class="OddSnap.UI.UpscaleResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:OddSnap.UI.Controls"
        Title="Upscale"
        Height="720" Width="1080"
        MinHeight="560" MinWidth="760"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        Icon="pack://application:,,,/Assets/oddsnap.ico"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <!-- Theme brushes are published by ApplyTheme() in the ctor from the
             UI.Theme token set (app-level copies exist via Theme.ApplyTo).
             Do not add hardcoded brush values here — they would be dark-only. -->
        <SolidColorBrush x:Key="UpscaleShimmerBrush" Color="#14FFFFFF"/>
        <SolidColorBrush x:Key="LoadingTextBrush" Color="#FFFFFFFF"/>
    </Window.Resources>

    <Border x:Name="RootBorder" CornerRadius="12" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <controls:OddSnapTitleBar Grid.Row="0"
                                      Title="Upscale"
                                      CloseRequested="TitleBar_CloseRequested"/>

            <Grid Grid.Row="1" Margin="14,0,14,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Padding="16"
                        CornerRadius="10"
                        Background="{DynamicResource ThemeCardBrush}"
                        BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                        BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel>
                            <TextBlock Text="Scale" FontSize="11" Opacity="0.5" Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                            <Slider x:Name="ScaleSlider"
                                    Margin="0,10,0,0"
                                    Minimum="2"
                                    Maximum="4"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    ValueChanged="ScaleSlider_ValueChanged"/>
                            <TextBlock x:Name="ScaleValueText"
                                       Margin="0,8,0,0"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Margin="0,18,0,0">
                            <TextBlock Text="Model" FontSize="11" Opacity="0.5" Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                            <ComboBox x:Name="ModelCombo"
                                      Margin="0,8,0,0"
                                      FontSize="12"
                                      Padding="8,5"
                                      SelectionChanged="ModelCombo_SelectionChanged"/>
                        </StackPanel>

                        <TextBlock Grid.Row="2"
                                   x:Name="StatusText"
                                   Margin="0,18,0,0"
                                   FontSize="11"
                                   Opacity="0.62"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                   Text="Pick a scale and model, then run a before-and-after preview."/>

                        <StackPanel Grid.Row="4">
                            <Button x:Name="UpscaleBtn"
                                    FontSize="12"
                                    Padding="18,10"
                                    Click="UpscaleBtn_Click"
                                    Background="{DynamicResource ThemeInputBackgroundBrush}"
                                    Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                    BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                                    BorderThickness="1"
                                    Content="Upscale"/>
                            <Grid Margin="0,8,0,0">
                                <Button x:Name="UseResultBtn"
                                        FontSize="12"
                                        Padding="18,10"
                                        Click="UseResultBtn_Click"
                                        Background="{DynamicResource ThemeInputBackgroundBrush}"
                                        Foreground="{DynamicResource ThemeTextPrimaryBrush}"
                                        BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                                        BorderThickness="1"
                                        IsEnabled="False">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Image x:Name="UseResultIcon"
                                               Width="15"
                                               Height="15"
                                               Margin="0,0,8,0"/>
                                        <TextBlock Text="Use Result" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Column="2"
                        Padding="14"
                        CornerRadius="10"
                        Background="{DynamicResource ThemeCardBrush}"
                        BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                        BorderThickness="1">
                    <Border x:Name="CompareSurface"
                            CornerRadius="10"
                            Background="{DynamicResource ThemeInputBackgroundBrush}"
                            BorderBrush="{DynamicResource ThemeInputBorderBrush}"
                            BorderThickness="1"
                            SizeChanged="CompareSurface_SizeChanged"
                            MouseLeftButtonDown="CompareSurface_MouseLeftButtonDown"
                            MouseLeftButtonUp="CompareSurface_MouseLeftButtonUp"
                            MouseMove="CompareSurface_MouseMove">
                        <Grid ClipToBounds="True">
                            <Grid x:Name="CompareImageLayer">
                                <Grid.Effect>
                                    <BlurEffect x:Name="CompareImageBlur" Radius="0"/>
                                </Grid.Effect>
                                <Image x:Name="CompareBeforeImage"
                                       Stretch="Fill"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>

                                <Image x:Name="CompareAfterImage"
                                       Stretch="Fill"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Visibility="Collapsed"
                                       RenderOptions.BitmapScalingMode="HighQuality">
                                    <Image.Clip>
                                        <RectangleGeometry x:Name="CompareAfterClip"/>
                                    </Image.Clip>
                                </Image>

                                <Border x:Name="CompareDivider"
                                        Width="2"
                                        Background="#F4FFFFFF"
                                        HorizontalAlignment="Left"
                                        Visibility="Collapsed"/>

                                <Grid x:Name="CompareHandle"
                                      Width="44"
                                      Height="44"
                                      HorizontalAlignment="Left"
                                      VerticalAlignment="Center"
                                      Margin="-22,0,0,0"
                                      Visibility="Collapsed"
                                      IsHitTestVisible="False">
                                    <Ellipse Fill="#F8FFFFFF"/>
                                    <Ellipse Margin="1.5"
                                             Fill="#10FFFFFF"
                                             Stroke="#33000000"
                                             StrokeThickness="1"/>
                                    <Path Stroke="#AA000000"
                                          StrokeThickness="2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          Data="M17,22 L12,17 M17,22 L12,27 M27,22 L32,17 M27,22 L32,27"/>
                                </Grid>
                            </Grid>

                            <Border x:Name="BeforeCornerLabel"
                                    Margin="14,12,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top"
                                    Background="#99111113"
                                    CornerRadius="4"
                                    Padding="7,2">
                                <TextBlock Text="Before"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Border>

                            <Border x:Name="AfterCornerLabel"
                                    Margin="0,12,14,0"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Background="#99111113"
                                    CornerRadius="4"
                                    Padding="7,2">
                                <TextBlock Text="After"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Border>

                            <Grid x:Name="AfterLoadingOverlay"
                                  Background="#66111113"
                                  Visibility="Collapsed">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock x:Name="AfterLoadingTitle"
                                               Margin="0,0,0,0"
                                               HorizontalAlignment="Center"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource LoadingTextBrush}"
                                               Text="Generating"/>
                                    <TextBlock x:Name="AfterLoadingSubtitle"
                                               Margin="0,4,0,0"
                                               HorizontalAlignment="Center"
                                               FontSize="10"
                                               Foreground="{StaticResource LoadingTextBrush}"
                                               Opacity="0.5"
                                               Text="Local model"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
