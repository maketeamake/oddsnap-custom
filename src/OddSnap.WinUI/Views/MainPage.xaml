<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="OddSnap.WinUI.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Padding="20" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
            <StackPanel Spacing="16">
                <StackPanel Spacing="6">
                    <TextBlock Text="OddSnap WinUI 3" FontSize="28" FontWeight="SemiBold" />
                    <TextBlock
                        Style="{StaticResource BodyTextStyle}"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="Migration shell for schema-driven settings and first-class background jobs." />
                </StackPanel>

                <TextBlock Text="Pages" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <ListView x:Name="PageList" SelectionChanged="PageList_SelectionChanged" Height="520" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Column="1">
            <StackPanel x:Name="ContentHost" Padding="28" Spacing="20" />
        </ScrollViewer>
    </Grid>
</Page>
